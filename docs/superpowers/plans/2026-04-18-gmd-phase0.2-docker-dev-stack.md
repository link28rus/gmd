# GMD Phase 0.2 — Docker Dev Stack Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Поднять полноценный dev-стек в Docker (PostgreSQL 16 + PostGIS + pg_cron, Redis 7, MinIO, Adminer), накатить первую Prisma-миграцию, подключить backend к БД/Redis, расширить `/healthz` до readiness-пробы, которая реально проверяет БД и Redis.

**Architecture:** Один `docker-compose.dev.yml` поднимает 4 сервиса локально (без TLS, localhost-порты). Инициализация БД через `init.sql` (PostGIS + pg_cron расширения). Backend получает `PrismaService` (Nest-модуль) и `RedisService` (ioredis), оба lifecycle-aware (`OnModuleInit`/`OnModuleDestroy`). `/healthz` уже работает → добавляем отдельный `/readyz`, который пингует БД и Redis. Prod-compose — только скелет (наполняем в 0.3).

**Tech Stack:** Docker Compose v2, PostgreSQL 16 (postgis/postgis:16-3.4), pg_cron (extension), Redis 7-alpine, MinIO (bitnami/minio), Adminer, Prisma 5.22, ioredis 5.x, NestJS lifecycle hooks.

**Deliverable после плана:**

- `docker compose -f infra/docker/docker-compose.dev.yml up -d` поднимает 4 сервиса
- `pnpm --filter @gmd/backend prisma:migrate:dev` успешно применяет миграцию к живой БД
- `curl http://localhost:3001/readyz` возвращает `{status:"ok", db:"up", redis:"up"}`
- Физически убедиться: `psql` подключается, `SELECT PostGIS_version()` работает, `SELECT pg_cron extension` видна
- MinIO UI доступен на http://localhost:9001 (логин minio/minio12345)
- Adminer на http://localhost:8080
- CHANGELOG обновлён, тег `v0.2.0`

**Репо-структура после плана:**

```
infra/
└── docker/
    ├── docker-compose.dev.yml
    ├── docker-compose.prod.yml          (скелет с комментариями TODO для 0.3)
    ├── .env.dev.example
    └── postgres/
        └── init.sql                      (PostGIS + pg_cron extensions)

apps/backend/
├── prisma/
│   ├── schema.prisma                     (обновлён: добавлены pg_cron/PostGIS extensions)
│   └── migrations/
│       └── 20260418xxxxxx_init/
│           └── migration.sql             (первая миграция)
├── src/
│   ├── prisma/
│   │   ├── prisma.module.ts              (NestJS module)
│   │   ├── prisma.service.ts             (lifecycle-aware)
│   │   └── prisma.service.spec.ts
│   ├── redis/
│   │   ├── redis.module.ts
│   │   ├── redis.service.ts
│   │   └── redis.service.spec.ts
│   ├── health/
│   │   ├── health.controller.ts          (обновлён — добавлен /readyz)
│   │   └── health.controller.spec.ts     (обновлён)
│   ├── app.module.ts                     (обновлён — подключены Prisma+Redis)
│   └── main.ts                           (без изменений)
└── .env                                  (обновлён: REDIS_URL)

CHANGELOG.md                              (добавлена запись v0.2.0)
package.json                              (добавлены scripts stack:up/down/logs)
```

---

## Prerequisites

- [ ] **Prerequisite: Docker Desktop для Windows запущен**

```bash
docker version 2>&1 | head -5
docker compose version 2>&1 | head -2
```

Должно показать Engine ≥ 24, Compose ≥ 2.20. Если Docker не запущен — стартовать Docker Desktop.

- [ ] **Prerequisite: порты свободны**

Нужны: **5432** (postgres), **6379** (redis), **9000/9001** (minio), **8080** (adminer), **3001** (backend, уже в использовании во время `pnpm dev`).

```bash
for p in 5432 6379 9000 9001 8080; do
  powershell -NoProfile -Command "(Get-NetTCPConnection -LocalPort $p -ErrorAction SilentlyContinue | Select-Object -First 1).State" 2>/dev/null || echo "$p: free"
done
```

Если 5432 занят (часто конфликт с локальным PostgreSQL-сервисом Windows) — либо остановить его, либо в `.env.dev` использовать другой хост-порт (например `54320:5432`) и скорректировать DATABASE_URL. В этом плане идём по умолчанию — 5432.

---

## Task 1: Структура `infra/docker/` + `.env.dev` template

**Files:**

- Create: `D:\Project\GMD\infra\docker\docker-compose.dev.yml`
- Create: `D:\Project\GMD\infra\docker\.env.dev.example`
- Create: `D:\Project\GMD\infra\docker\postgres\init.sql`
- Create: `D:\Project\GMD\infra\docker\docker-compose.prod.yml` (скелет)
- Modify: `D:\Project\GMD\.gitignore` (добавить `infra/docker/.env.dev`)

- [ ] **Step 1.1: Создать директорию `infra/docker/postgres/`**

```bash
mkdir -p /d/Project/GMD/infra/docker/postgres
```

- [ ] **Step 1.2: Создать `infra/docker/.env.dev.example`**

```env
# Development Docker environment
# Copy to .env.dev and adjust if needed

POSTGRES_USER=gmd
POSTGRES_PASSWORD=gmd_dev_password
POSTGRES_DB=gmd_dev
POSTGRES_PORT=5432

REDIS_PORT=6379

MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio12345
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

ADMINER_PORT=8080
```

- [ ] **Step 1.3: Создать `infra/docker/postgres/init.sql`**

Этот скрипт выполняется при первом запуске контейнера (volume пуст). Создаёт расширения.

```sql
-- PostGIS for geospatial queries (geofences, locations)
CREATE EXTENSION IF NOT EXISTS postgis;
CREATE EXTENSION IF NOT EXISTS postgis_topology;

-- pg_cron for scheduled cleanup (30-day location retention, etc.)
-- Note: pg_cron requires pg_cron added to shared_preload_libraries in postgresql.conf.
-- The official postgis image doesn't bundle pg_cron; we'll install it in the Dockerfile
-- in a later task if needed. For Phase 0.2 we only install PostGIS; pg_cron slots in
-- during Phase 1 (when retention jobs are needed).

-- Verify PostGIS is available
SELECT PostGIS_version();
```

(Примечание: pg_cron встраиваем в Phase 1 через кастомный Dockerfile — он не идёт в комплекте с postgis/postgis image.)

- [ ] **Step 1.4: Создать `infra/docker/docker-compose.dev.yml`**

```yaml
name: gmd-dev

services:
  postgres:
    image: postgis/postgis:16-3.4
    container_name: gmd-postgres-dev
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-gmd}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-gmd_dev_password}
      POSTGRES_DB: ${POSTGRES_DB:-gmd_dev}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/10-init.sql:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-gmd} -d ${POSTGRES_DB:-gmd_dev}']
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: gmd-redis-dev
    restart: unless-stopped
    ports:
      - '${REDIS_PORT:-6379}:6379'
    volumes:
      - redis_data:/data
    command: ['redis-server', '--appendonly', 'yes']
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 5

  minio:
    image: bitnami/minio:2024
    container_name: gmd-minio-dev
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minio}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minio12345}
      MINIO_DEFAULT_BUCKETS: gmd-uploads
    ports:
      - '${MINIO_API_PORT:-9000}:9000'
      - '${MINIO_CONSOLE_PORT:-9001}:9001'
    volumes:
      - minio_data:/bitnami/minio/data
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:9000/minio/health/live']
      interval: 10s
      timeout: 5s
      retries: 5

  adminer:
    image: adminer:latest
    container_name: gmd-adminer-dev
    restart: unless-stopped
    ports:
      - '${ADMINER_PORT:-8080}:8080'
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
  redis_data:
  minio_data:
```

- [ ] **Step 1.5: Создать `infra/docker/docker-compose.prod.yml` (скелет)**

Этот файл только с TODO-комментариями — наполняем в Phase 0.3.

```yaml
# Production docker-compose for GMD — СКЕЛЕТ, наполняется в Phase 0.3
#
# Отличия от dev:
# - Caddy reverse proxy с TLS (Let's Encrypt) для gmd.link28rus.ru
# - postgres без пробрасывания порта наружу (доступ только из compose-сети)
# - redis без пробрасывания порта
# - minio за Caddy на s3.gmd.link28rus.ru
# - GlitchTip (self-hosted Sentry API-совместимый)
# - Uptime Kuma на status.gmd.link28rus.ru
# - Grafana + Loki + Prometheus для метрик и логов
# - Ежедневный backup-сервис (pg_dump + age + загрузка в Yandex Object Storage)
# - Backend и Web контейнеры (сейчас runs out of compose)
#
# TODO (Phase 0.3):
# - [ ] Caddy
# - [ ] GlitchTip
# - [ ] Uptime Kuma
# - [ ] Backup-cron
# - [ ] Backend production image
# - [ ] Web production image
# - [ ] Grafana/Loki/Prometheus

name: gmd-prod

services: {}
```

- [ ] **Step 1.6: Добавить `.env.dev` в `.gitignore`**

Открыть `D:\Project\GMD\.gitignore`, найти секцию `# Environment` и добавить строку `infra/docker/.env.dev` сразу после существующих `.env` правил. Убедиться, что `.env.dev.example` НЕ игнорируется.

Вручную: после строки `!.env.example` добавить:

```
infra/docker/.env.dev
!infra/docker/.env.dev.example
```

- [ ] **Step 1.7: Создать рабочий `infra/docker/.env.dev`**

```bash
cd /d/Project/GMD/infra/docker
cp .env.dev.example .env.dev
```

Проверить, что он игнорируется:

```bash
cd /d/Project/GMD
git check-ignore infra/docker/.env.dev && echo "IGNORED_OK" || echo "NOT_IGNORED_PROBLEM"
```

Ожидаемо: `IGNORED_OK`. Если нет — починить .gitignore.

- [ ] **Step 1.8: Коммит**

```bash
cd /d/Project/GMD
git add infra/ .gitignore
git status
git commit -m "feat(infra): add docker-compose dev stack skeleton (postgres, redis, minio, adminer)"
```

---

## Task 2: Поднять стек и smoke-тест каждого сервиса

**Files:** (никаких, только команды)

- [ ] **Step 2.1: Запустить стек**

```bash
cd /d/Project/GMD/infra/docker
docker compose --env-file .env.dev -f docker-compose.dev.yml up -d 2>&1 | tail -10
```

Ожидаемо: 4 контейнера стартуют. Первый запуск скачает образы (~300-500 MB). Может занять 2-3 минуты.

- [ ] **Step 2.2: Дождаться health всех сервисов**

```bash
cd /d/Project/GMD/infra/docker
for i in 1 2 3 4 5 6; do
  echo "--- attempt $i ---"
  docker compose --env-file .env.dev -f docker-compose.dev.yml ps --format "table {{.Name}}\t{{.Status}}"
  if docker compose --env-file .env.dev -f docker-compose.dev.yml ps --format "{{.Status}}" | grep -cv "healthy\|running\|Up" | grep -q "^0$"; then
    echo "ALL_UP"
    break
  fi
  sleep 10
done
```

Ожидаемо: все 4 сервиса в статусе `healthy` или `running` (Adminer не имеет healthcheck, просто `running`). Если MinIO долго стартует — нормально, дать ему до 60 сек.

- [ ] **Step 2.3: Smoke-тест Postgres**

```bash
docker exec gmd-postgres-dev psql -U gmd -d gmd_dev -c "SELECT PostGIS_version();" 2>&1
```

Ожидаемо: одна строка с версией PostGIS (`3.4 USE_GEOS=1 ...`).

- [ ] **Step 2.4: Smoke-тест Redis**

```bash
docker exec gmd-redis-dev redis-cli PING
docker exec gmd-redis-dev redis-cli SET smoke-test hello
docker exec gmd-redis-dev redis-cli GET smoke-test
docker exec gmd-redis-dev redis-cli DEL smoke-test
```

Ожидаемо: `PONG`, `OK`, `hello`, `1`.

- [ ] **Step 2.5: Smoke-тест MinIO**

```bash
curl -sS -m 5 http://localhost:9000/minio/health/live -o /dev/null -w "MinIO API: %{http_code}\n"
curl -sS -m 5 http://localhost:9001 -o /dev/null -w "MinIO Console: %{http_code}\n"
```

Ожидаемо: MinIO API returns 200, Console returns 200 (HTML login page).

- [ ] **Step 2.6: Smoke-тест Adminer**

```bash
curl -sS -m 5 http://localhost:8080 -o /dev/null -w "Adminer: %{http_code}\n"
```

Ожидаемо: 200.

- [ ] **Step 2.7: Коммитить нечего — проверка прошла. Переходим к Task 3.**

---

## Task 3: Root-скрипты `stack:up` / `stack:down` / `stack:logs`

**Files:**

- Modify: `D:\Project\GMD\package.json` (добавить scripts)

- [ ] **Step 3.1: Прочитать текущий `package.json`**

Прочитать файл. Найти секцию `scripts`. Она содержит: `dev`, `build`, `test`, `lint`, `typecheck`, `format`, `format:check`, `prepare`. Не трогать их.

- [ ] **Step 3.2: Добавить stack-скрипты**

Добавить внутрь `scripts` (перед закрывающей `}` секции):

```
"stack:up": "docker compose --env-file infra/docker/.env.dev -f infra/docker/docker-compose.dev.yml up -d",
"stack:down": "docker compose --env-file infra/docker/.env.dev -f infra/docker/docker-compose.dev.yml down",
"stack:logs": "docker compose --env-file infra/docker/.env.dev -f infra/docker/docker-compose.dev.yml logs -f",
"stack:ps": "docker compose --env-file infra/docker/.env.dev -f infra/docker/docker-compose.dev.yml ps",
"stack:reset": "docker compose --env-file infra/docker/.env.dev -f infra/docker/docker-compose.dev.yml down -v"
```

Внимательно: корректные запятые между скриптами.

- [ ] **Step 3.3: Проверить работу**

```bash
cd /d/Project/GMD
pnpm stack:ps
```

Ожидаемо: выводит список 4 контейнеров в running/healthy.

- [ ] **Step 3.4: Коммит**

```bash
cd /d/Project/GMD
git add package.json
git commit -m "chore: add stack:up/down/logs/reset scripts"
```

---

## Task 4: Первая Prisma-миграция

**Files:**

- Modify: `D:\Project\GMD\apps\backend\.env` (убедиться что DATABASE_URL указывает на живую БД)
- Create: `D:\Project\GMD\apps\backend\prisma\migrations\...\migration.sql` (через CLI)
- Modify: `D:\Project\GMD\apps\backend\prisma\schema.prisma` (без изменений, но убедиться что стабильна)

- [ ] **Step 4.1: Убедиться, что `.env` с правильным DATABASE_URL**

```bash
cd /d/Project/GMD/apps/backend
cat .env
```

Ожидаемо: `DATABASE_URL="postgresql://gmd:gmd@localhost:5432/gmd_dev?schema=public"`.

Но пароль в Docker — `gmd_dev_password` (из `.env.dev.example`). Нужно синхронизировать. Обновить `.env`:

```
DATABASE_URL="postgresql://gmd:gmd_dev_password@localhost:5432/gmd_dev?schema=public"
PORT=3001
NODE_ENV=development
REDIS_URL="redis://localhost:6379"
```

Также обновить `.env.example`, чтобы пример совпадал:

```
# Development
DATABASE_URL="postgresql://gmd:gmd_dev_password@localhost:5432/gmd_dev?schema=public"
PORT=3001
NODE_ENV=development
REDIS_URL="redis://localhost:6379"
```

- [ ] **Step 4.2: Запустить `prisma migrate dev` для создания первой миграции**

```bash
cd /d/Project/GMD/apps/backend
pnpm prisma migrate dev --name init 2>&1 | tail -15
```

Ожидаемо:

- Prisma применяет миграцию к живой БД
- Создаётся файл `prisma/migrations/20260418XXXXXX_init/migration.sql`
- Prisma Client перегенерирован
- «The following migration(s) have been created and applied from new schema changes: 20260418XXXXXX_init»

- [ ] **Step 4.3: Проверить таблицу в БД напрямую**

```bash
docker exec gmd-postgres-dev psql -U gmd -d gmd_dev -c "\d users"
```

Ожидаемо: описание таблицы `users` с колонками id (text PRIMARY KEY), email (text UNIQUE), createdAt, updatedAt.

- [ ] **Step 4.4: Убедиться, что migrations/ файл выглядит разумно**

```bash
ls /d/Project/GMD/apps/backend/prisma/migrations/
cat /d/Project/GMD/apps/backend/prisma/migrations/*/migration.sql
```

Ожидаемо: `CREATE TABLE "users" ...` в SQL.

- [ ] **Step 4.5: Коммит (миграция + обновлённый .env.example)**

```bash
cd /d/Project/GMD
git add apps/backend/prisma/migrations/ apps/backend/.env.example
git status
git commit -m "feat(backend): add initial Prisma migration for User stub"
```

Проверить, что `.env` (НЕ example) не попал:

```bash
git log -1 --stat | grep -E 'apps/backend/\.env(\s|$)' && echo "LEAK_DETECTED_STOP" || echo "NO_LEAK_OK"
```

Если `LEAK_DETECTED_STOP` — немедленно откатить и исправить.

---

## Task 5: PrismaService для NestJS (TDD)

**Files:**

- Create: `D:\Project\GMD\apps\backend\src\prisma\prisma.service.ts`
- Create: `D:\Project\GMD\apps\backend\src\prisma\prisma.service.spec.ts`
- Create: `D:\Project\GMD\apps\backend\src\prisma\prisma.module.ts`

- [ ] **Step 5.1: Написать тест первым (TDD)**

Создать `apps/backend/src/prisma/prisma.service.spec.ts`:

```typescript
import { Test } from '@nestjs/testing';
import type { TestingModule } from '@nestjs/testing';
import { PrismaService } from './prisma.service';

describe('PrismaService', () => {
  let service: PrismaService;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      providers: [PrismaService],
    }).compile();

    service = module.get<PrismaService>(PrismaService);
  });

  it('should be defined', () => {
    expect(service).toBeDefined();
  });

  it('should expose $connect and $disconnect methods', () => {
    expect(typeof service.$connect).toBe('function');
    expect(typeof service.$disconnect).toBe('function');
  });

  it('should implement OnModuleInit interface', () => {
    expect(typeof service.onModuleInit).toBe('function');
  });

  it('should implement OnModuleDestroy interface', () => {
    expect(typeof service.onModuleDestroy).toBe('function');
  });
});
```

- [ ] **Step 5.2: Запустить — увидеть FAIL**

```bash
cd /d/Project/GMD
pnpm --filter @gmd/backend test 2>&1 | tail -10
```

Ожидаемо: FAIL — `Cannot find module './prisma.service'`.

- [ ] **Step 5.3: Создать `apps/backend/src/prisma/prisma.service.ts`**

```typescript
import { Injectable, OnModuleDestroy, OnModuleInit, Logger } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';

@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy {
  private readonly logger = new Logger(PrismaService.name);

  async onModuleInit(): Promise<void> {
    await this.$connect();
    this.logger.log('Prisma connected');
  }

  async onModuleDestroy(): Promise<void> {
    await this.$disconnect();
    this.logger.log('Prisma disconnected');
  }
}
```

- [ ] **Step 5.4: Создать `apps/backend/src/prisma/prisma.module.ts`**

```typescript
import { Global, Module } from '@nestjs/common';
import { PrismaService } from './prisma.service';

@Global()
@Module({
  providers: [PrismaService],
  exports: [PrismaService],
})
export class PrismaModule {}
```

- [ ] **Step 5.5: Запустить — увидеть PASS**

```bash
cd /d/Project/GMD
pnpm --filter @gmd/backend test 2>&1 | tail -10
```

Ожидаемо: 4 PASS в PrismaService + 2 PASS в HealthController = 6 total.

- [ ] **Step 5.6: Коммит**

```bash
cd /d/Project/GMD
git add apps/backend/src/prisma/
git commit -m "feat(backend): add PrismaService with lifecycle hooks (TDD)"
```

---

## Task 6: RedisService для NestJS (TDD)

**Files:**

- Create: `D:\Project\GMD\apps\backend\src\redis\redis.service.ts`
- Create: `D:\Project\GMD\apps\backend\src\redis\redis.service.spec.ts`
- Create: `D:\Project\GMD\apps\backend\src\redis\redis.module.ts`

- [ ] **Step 6.1: Установить `ioredis`**

```bash
cd /d/Project/GMD
pnpm --filter @gmd/backend add ioredis
```

- [ ] **Step 6.2: Написать тест первым**

Создать `apps/backend/src/redis/redis.service.spec.ts`:

```typescript
import { Test } from '@nestjs/testing';
import type { TestingModule } from '@nestjs/testing';
import { RedisService } from './redis.service';

describe('RedisService', () => {
  let service: RedisService;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      providers: [RedisService],
    }).compile();

    service = module.get<RedisService>(RedisService);
  });

  afterEach(async () => {
    await service.onModuleDestroy();
  });

  it('should be defined', () => {
    expect(service).toBeDefined();
  });

  it('should expose getClient()', () => {
    expect(typeof service.getClient).toBe('function');
  });

  it('should implement lifecycle hooks', () => {
    expect(typeof service.onModuleInit).toBe('function');
    expect(typeof service.onModuleDestroy).toBe('function');
  });
});
```

Тест НЕ проверяет реальное подключение к Redis (для unit-уровня) — только shape сервиса. Интеграция с живым Redis покрывается Task 7 через `/readyz`.

- [ ] **Step 6.3: Запустить — увидеть FAIL**

```bash
cd /d/Project/GMD
pnpm --filter @gmd/backend test 2>&1 | tail -10
```

Ожидаемо: FAIL (`Cannot find module './redis.service'`).

- [ ] **Step 6.4: Создать `apps/backend/src/redis/redis.service.ts`**

```typescript
import { Injectable, OnModuleDestroy, OnModuleInit, Logger } from '@nestjs/common';
import Redis from 'ioredis';
import type { Redis as RedisClient } from 'ioredis';

@Injectable()
export class RedisService implements OnModuleInit, OnModuleDestroy {
  private readonly logger = new Logger(RedisService.name);
  private client: RedisClient | null = null;

  async onModuleInit(): Promise<void> {
    const url = process.env.REDIS_URL ?? 'redis://localhost:6379';
    this.client = new Redis(url, {
      lazyConnect: false,
      maxRetriesPerRequest: 3,
    });
    this.client.on('error', (err) => this.logger.error('Redis error', err.message));
    this.logger.log(`Redis client initialized → ${url}`);
  }

  async onModuleDestroy(): Promise<void> {
    if (this.client) {
      await this.client.quit();
      this.client = null;
      this.logger.log('Redis disconnected');
    }
  }

  getClient(): RedisClient {
    if (!this.client) {
      throw new Error('Redis client not initialized — RedisService.onModuleInit not called');
    }
    return this.client;
  }
}
```

- [ ] **Step 6.5: Создать `apps/backend/src/redis/redis.module.ts`**

```typescript
import { Global, Module } from '@nestjs/common';
import { RedisService } from './redis.service';

@Global()
@Module({
  providers: [RedisService],
  exports: [RedisService],
})
export class RedisModule {}
```

- [ ] **Step 6.6: Запустить — увидеть PASS**

```bash
cd /d/Project/GMD
pnpm --filter @gmd/backend test 2>&1 | tail -10
```

Ожидаемо: 3 PASS в RedisService + 4 PrismaService + 2 Health = 9 total.

- [ ] **Step 6.7: Коммит**

```bash
cd /d/Project/GMD
git add apps/backend/src/redis/ apps/backend/package.json pnpm-lock.yaml
git commit -m "feat(backend): add RedisService with lifecycle hooks (TDD)"
```

---

## Task 7: Подключить Prisma + Redis в AppModule, расширить HealthController `/readyz` (TDD)

**Files:**

- Modify: `D:\Project\GMD\apps\backend\src\app.module.ts`
- Modify: `D:\Project\GMD\apps\backend\src\health\health.controller.ts`
- Modify: `D:\Project\GMD\apps\backend\src\health\health.controller.spec.ts`

- [ ] **Step 7.1: Обновить `app.module.ts` — подключить Prisma и Redis**

Перезаписать `apps/backend/src/app.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { HealthController } from './health/health.controller';
import { PrismaModule } from './prisma/prisma.module';
import { RedisModule } from './redis/redis.module';

@Module({
  imports: [PrismaModule, RedisModule],
  controllers: [HealthController],
  providers: [],
})
export class AppModule {}
```

- [ ] **Step 7.2: Обновить тест `health.controller.spec.ts` — добавить проверки `/readyz`**

Перезаписать `apps/backend/src/health/health.controller.spec.ts`:

```typescript
import { Test } from '@nestjs/testing';
import type { TestingModule } from '@nestjs/testing';
import { HealthController } from './health.controller';
import { PrismaService } from '../prisma/prisma.service';
import { RedisService } from '../redis/redis.service';

describe('HealthController', () => {
  let controller: HealthController;
  let prismaMock: Partial<PrismaService>;
  let redisMock: Partial<RedisService>;

  beforeEach(async () => {
    prismaMock = {
      $queryRaw: jest.fn().mockResolvedValue([{ ok: 1 }]),
    } as unknown as Partial<PrismaService>;

    redisMock = {
      getClient: jest.fn().mockReturnValue({ ping: jest.fn().mockResolvedValue('PONG') }),
    } as unknown as Partial<RedisService>;

    const module: TestingModule = await Test.createTestingModule({
      controllers: [HealthController],
      providers: [
        { provide: PrismaService, useValue: prismaMock },
        { provide: RedisService, useValue: redisMock },
      ],
    }).compile();

    controller = module.get<HealthController>(HealthController);
  });

  describe('/healthz (liveness)', () => {
    it('should return {status: "ok"}', () => {
      const result = controller.check();
      expect(result.status).toBe('ok');
    });

    it('should include timestamp as ISO string', () => {
      const result = controller.check();
      expect(result).toHaveProperty('timestamp');
      expect(() => new Date(result.timestamp)).not.toThrow();
    });
  });

  describe('/readyz (readiness)', () => {
    it('should return ok when both db and redis respond', async () => {
      const result = await controller.ready();
      expect(result.status).toBe('ok');
      expect(result.db).toBe('up');
      expect(result.redis).toBe('up');
    });

    it('should return db:"down" and status:"error" if Prisma throws', async () => {
      (prismaMock.$queryRaw as jest.Mock).mockRejectedValueOnce(new Error('connection refused'));
      const result = await controller.ready();
      expect(result.status).toBe('error');
      expect(result.db).toBe('down');
      expect(result.redis).toBe('up');
    });

    it('should return redis:"down" and status:"error" if Redis throws', async () => {
      const pingMock = jest.fn().mockRejectedValueOnce(new Error('timeout'));
      (redisMock.getClient as jest.Mock).mockReturnValueOnce({ ping: pingMock });
      const result = await controller.ready();
      expect(result.status).toBe('error');
      expect(result.db).toBe('up');
      expect(result.redis).toBe('down');
    });
  });
});
```

- [ ] **Step 7.3: Запустить тесты — увидеть FAIL (ready() не существует)**

```bash
cd /d/Project/GMD
pnpm --filter @gmd/backend test 2>&1 | tail -15
```

Ожидаемо: FAIL — тесты на `/readyz` падают, потому что `controller.ready()` ещё нет.

- [ ] **Step 7.4: Обновить `health.controller.ts` — добавить `/readyz`**

Перезаписать `apps/backend/src/health/health.controller.ts`:

```typescript
import { Controller, Get } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';
import { RedisService } from '../redis/redis.service';

type LivenessResponse = {
  status: 'ok';
  timestamp: string;
};

type ReadinessResponse = {
  status: 'ok' | 'error';
  timestamp: string;
  db: 'up' | 'down';
  redis: 'up' | 'down';
};

@Controller()
export class HealthController {
  constructor(
    private readonly prisma: PrismaService,
    private readonly redis: RedisService,
  ) {}

  @Get('healthz')
  check(): LivenessResponse {
    return {
      status: 'ok',
      timestamp: new Date().toISOString(),
    };
  }

  @Get('readyz')
  async ready(): Promise<ReadinessResponse> {
    const [dbStatus, redisStatus] = await Promise.all([this.checkDb(), this.checkRedis()]);

    const status: 'ok' | 'error' = dbStatus === 'up' && redisStatus === 'up' ? 'ok' : 'error';

    return {
      status,
      timestamp: new Date().toISOString(),
      db: dbStatus,
      redis: redisStatus,
    };
  }

  private async checkDb(): Promise<'up' | 'down'> {
    try {
      await this.prisma.$queryRaw`SELECT 1`;
      return 'up';
    } catch {
      return 'down';
    }
  }

  private async checkRedis(): Promise<'up' | 'down'> {
    try {
      const response = await this.redis.getClient().ping();
      return response === 'PONG' ? 'up' : 'down';
    } catch {
      return 'down';
    }
  }
}
```

- [ ] **Step 7.5: Запустить тесты — всё должно пройти**

```bash
cd /d/Project/GMD
pnpm --filter @gmd/backend test 2>&1 | tail -15
```

Ожидаемо: всё PASS. Общее число тестов: 2 (liveness) + 3 (readyz) + 4 (Prisma) + 3 (Redis) = 12.

- [ ] **Step 7.6: Интеграционный smoke — живой backend против живых БД/Redis**

Убедиться, что стек поднят:

```bash
cd /d/Project/GMD
pnpm stack:ps | grep -c healthy || pnpm stack:up
```

Запустить backend:

```bash
cd /d/Project/GMD
pnpm --filter @gmd/backend dev > /tmp/backend.log 2>&1 &
DEVPID=$!
sleep 12
echo "=== /healthz ==="
curl -sS -m 5 http://localhost:3001/healthz
echo
echo "=== /readyz ==="
curl -sS -m 5 http://localhost:3001/readyz
echo
echo "=== LOG TAIL ==="
tail -20 /tmp/backend.log
kill $DEVPID 2>/dev/null
sleep 2
```

Ожидаемо:

- `/healthz` → `{"status":"ok","timestamp":"..."}`
- `/readyz` → `{"status":"ok","timestamp":"...","db":"up","redis":"up"}`
- В логах "Prisma connected" и "Redis client initialized"

Если `/readyz` возвращает `db:"down"` или `redis:"down"` — проверить `apps/backend/.env` на правильный пароль/URL + `docker ps` что контейнеры живы.

- [ ] **Step 7.7: Коммит**

```bash
cd /d/Project/GMD
git add apps/backend/src/app.module.ts apps/backend/src/health/
git commit -m "feat(backend): wire Prisma+Redis in AppModule and add /readyz check (TDD)"
```

---

## Task 8: Smoke MinIO — создать bucket и загрузить файл

**Files:** (временный скрипт для проверки)

- [ ] **Step 8.1: Проверить, что bucket `gmd-uploads` создался (из `MINIO_DEFAULT_BUCKETS`)**

```bash
# Установить mc (MinIO client) одноразово через docker run
docker run --rm --network host minio/mc alias set local http://localhost:9000 minio minio12345
docker run --rm --network host minio/mc ls local/
```

Ожидаемо: `gmd-uploads/` присутствует.

- [ ] **Step 8.2: Загрузить тестовый файл**

```bash
echo "hello from gmd" > /tmp/gmd-smoke.txt
docker run --rm --network host -v /tmp:/tmp minio/mc cp /tmp/gmd-smoke.txt local/gmd-uploads/smoke.txt
docker run --rm --network host minio/mc ls local/gmd-uploads/
docker run --rm --network host minio/mc cat local/gmd-uploads/smoke.txt
```

Ожидаемо: файл загружен, `cat` показывает `hello from gmd`.

- [ ] **Step 8.3: Удалить тестовый файл**

```bash
docker run --rm --network host minio/mc rm local/gmd-uploads/smoke.txt
rm /tmp/gmd-smoke.txt
```

- [ ] **Step 8.4: Коммитить нечего. Переходим к Task 9.**

---

## Task 9: Обновить документацию и CLAUDE.md команды

**Files:**

- Modify: `D:\Project\GMD\CLAUDE.md` (секция «Команды (dev)»)
- Modify: `D:\Project\GMD\README.md` (добавить раздел про docker-стек)

- [ ] **Step 9.1: Обновить секцию «Команды (dev)» в `CLAUDE.md`**

Прочитать `CLAUDE.md`. Найти блок `## Команды (dev)` и всё, что под ним до следующего `##`. Заменить блок:

````markdown
## Команды (dev)

### Начальная установка

```bash
pnpm install              # JS/TS-зависимости
melos bootstrap           # Dart/Flutter-зависимости
```
````

### Docker-стек (Postgres + PostGIS, Redis, MinIO, Adminer)

```bash
pnpm stack:up             # поднять все сервисы
pnpm stack:down           # остановить (volumes сохраняются)
pnpm stack:reset          # остановить + удалить volumes (сброс БД!)
pnpm stack:logs           # следить за логами всех сервисов
pnpm stack:ps             # статус сервисов
```

Порты: Postgres `5432`, Redis `6379`, MinIO API `9000`, MinIO Console `9001`, Adminer `8080`.

### Приложения

```bash
pnpm dev                  # backend (3001) + web (3000) параллельно
pnpm build                # сборка всех JS/TS workspace-пакетов
pnpm test                 # Jest по всем пакетам
pnpm lint
pnpm typecheck

# Только backend
pnpm --filter @gmd/backend dev
pnpm --filter @gmd/backend prisma migrate dev --name <name>
pnpm --filter @gmd/backend prisma studio

# Mobile
cd apps/mobile-parent && flutter run
cd apps/mobile-child && flutter run
melos run analyze
```

### Healthchecks

- Backend liveness: http://localhost:3001/healthz
- Backend readiness (DB+Redis): http://localhost:3001/readyz
- Web: http://localhost:3000/api/healthz

````

- [ ] **Step 9.2: Обновить README.md — добавить раздел «Docker dev stack» после «Быстрый старт»**

Вставить после блока с `pnpm install && pnpm dev`:

```markdown
## Dev-стек в Docker

```bash
pnpm stack:up             # Postgres+PostGIS, Redis, MinIO, Adminer
pnpm stack:down
pnpm stack:logs
````

- Postgres: `localhost:5432` (user `gmd`, db `gmd_dev`, см. `infra/docker/.env.dev.example`)
- Redis: `localhost:6379`
- MinIO: API `localhost:9000`, Console `localhost:9001` (minio / minio12345)
- Adminer: http://localhost:8080

````

- [ ] **Step 9.3: Коммит**

```bash
cd /d/Project/GMD
git add CLAUDE.md README.md
git commit -m "docs: document docker dev stack commands"
````

---

## Task 10: CHANGELOG v0.2.0 + тег

**Files:**

- Modify: `D:\Project\GMD\CHANGELOG.md`

- [ ] **Step 10.1: Обновить CHANGELOG**

Прочитать текущий `D:\Project\GMD\CHANGELOG.md`. Найти точную строку `## v0.1.0 — 2026-04-18` и использовать Edit tool с `old_string = "## v0.1.0 — 2026-04-18"` и `new_string` вот таким блоком:

```
## v0.2.0 — 2026-04-18

### Новые возможности
- **Docker dev-стек** — `pnpm stack:up` поднимает PostgreSQL 16 + PostGIS, Redis 7, MinIO, Adminer одной командой
- **Prisma-миграции работают** — первая миграция применена к живой БД, таблица `users` создана
- **Readiness-проба** — новый эндпоинт `GET /readyz` возвращает `{status, db, redis}` и реально пингует БД и Redis
- **PrismaService + RedisService** — NestJS-модули с lifecycle-хуками (`OnModuleInit` / `OnModuleDestroy`)

### Улучшения
- **Скрипты стека** — `stack:up/down/logs/ps/reset` в root package.json
- **Документация** — CLAUDE.md и README описывают dev-команды и порты стека

### Изменения
- chore(infra): docker-compose.prod.yml скелет с TODO (наполняется в Phase 0.3)
- chore(backend): `.env.example` обновлён (REDIS_URL + корректный пароль PG)

---

## v0.1.0 — 2026-04-18
```

Это вставит новый раздел v0.2.0 ПЕРЕД существующим v0.1.0, сохранив всё остальное содержимое файла нетронутым (включая раздел `[Unreleased]` сверху и содержимое v0.1.0 ниже).

После Edit — прочитать файл и убедиться глазами, что:

- `[Unreleased]` блок сверху не повреждён
- Новый v0.2.0 между `---` и `## v0.1.0`
- Блок v0.1.0 с подзаголовками «### Изменения» и списком пунктов сохранился полностью (он шёл сразу после заменяемой строки)
- HTML-коммент-шаблон в конце файла остался

- [ ] **Step 10.2: Коммит CHANGELOG**

```bash
cd /d/Project/GMD
git add CHANGELOG.md
git commit -m "docs(changelog): release v0.2.0 — docker dev stack + Prisma/Redis wiring"
```

- [ ] **Step 10.3: Аннотированный тег**

```bash
cd /d/Project/GMD
git tag -a v0.2.0 -m "v0.2.0 — docker dev stack + Prisma/Redis wiring + /readyz"
git tag --list
git log --oneline | head -15
```

Ожидаемо: тег `v0.2.0` создан, в логе примерно 9 + 8 = 17 коммитов.

---

## Task 11: Финальная проверка

- [ ] **Step 11.1: Всё-всё-всё — зелёное?**

```bash
cd /d/Project/GMD
pnpm stack:up
sleep 10
pnpm lint && echo OK_LINT
pnpm typecheck && echo OK_TYPECHECK
pnpm test && echo OK_TEST
```

Ожидаемо: `OK_LINT`, `OK_TYPECHECK`, `OK_TEST`.

- [ ] **Step 11.2: Интеграционный smoke**

```bash
cd /d/Project/GMD
pnpm dev > /tmp/dev-final.log 2>&1 &
DEVPID=$!
sleep 25
echo "=== backend /healthz ==="
curl -sS -m 5 http://localhost:3001/healthz
echo
echo "=== backend /readyz ==="
curl -sS -m 5 http://localhost:3001/readyz
echo
echo "=== web /api/healthz ==="
curl -sS -m 5 http://localhost:3000/api/healthz || curl -sS -m 5 http://localhost:3005/api/healthz
echo
kill $DEVPID 2>/dev/null
powershell -NoProfile -Command "Get-Process -Name 'node' -ErrorAction SilentlyContinue | Stop-Process -Force -ErrorAction SilentlyContinue" 2>/dev/null
sleep 3
```

Ожидаемо: все три healthz возвращают ok; readyz содержит `db:up, redis:up`.

- [ ] **Step 11.3: Отчёт**

Вывести:

- кол-во коммитов / тегов
- список live-контейнеров (`docker ps`)
- размер postgres_data volume (`docker system df -v | grep postgres_data`)
- что осталось для MVP (Phase 0.3 Server, 0.4 CI/CD, 0.5 Legal, Phase 1+)

---

## Definition of Done

- [x] `pnpm stack:up` поднимает все 4 dev-сервиса, healthchecks зелёные
- [x] `psql -U gmd -d gmd_dev -c "SELECT PostGIS_version()"` работает
- [x] `pnpm --filter @gmd/backend prisma migrate dev` прошёл, таблица `users` существует
- [x] `curl /readyz` возвращает `db:"up", redis:"up"` при живом стеке
- [x] `curl /readyz` возвращает `db:"down"` если `docker compose stop postgres` (ручная проверка)
- [x] `pnpm lint && pnpm typecheck && pnpm test` — всё зелёное; тестов 12+
- [x] Тег `v0.2.0` создан локально
- [x] CHANGELOG обновлён по принятому формату (с жирными заголовками для user-facing, conventional для техчейнджей)
- [x] Нет секретов в git (проверить `git log -p | grep -iE 'password|secret|token'`)

## Что НЕ входит в этот план (отложено)

| План       | Что                                                                                |
| ---------- | ---------------------------------------------------------------------------------- |
| 0.3 Server | Деплой compose'а на 192.168.1.23, hardening SSH, non-root user, UFW, fail2ban      |
| 0.3 Server | Caddy с TLS для `gmd.link28rus.ru`, проброс портов на роутере                      |
| 0.3 Server | GlitchTip, Uptime Kuma (мониторинг прода)                                          |
| 0.3 Server | pg_dump бэкап-крон + шифрование age + Yandex Object Storage                        |
| 0.3 Server | pg_cron extension (custom Dockerfile) — нужен для retention-джоб в Phase 1         |
| 0.4 CI/CD  | GitHub private repo, Actions: lint/typecheck/test на PR, SSH-деплой на main        |
| 0.5 Legal  | Политика конфиденциальности, согласие ПДн, чеклист РКН, `DELETE /me`               |
| Phase 1    | Реальные модели: User (phone+OTP), Family, ChildDevice, Invite, Location, Geofence |
| Phase 1    | OpenAPI spec + codegen                                                             |
| Phase 1    | JWT auth + device-token                                                            |
| Phase 1    | pg_cron retention: locations старше 30 дней, geofence_events старше 90             |
