# GMD Phase 1.1-web — Login + Cabinet Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Реализовать в Next.js 15 App Router login-форму (email + OTP) и минимальный кабинет для ручного тестирования backend-auth через браузер.

**Architecture:** 3 страницы (`/`, `/login`, `/cabinet`) + 5 Next.js API-routes как прокси на backend (`/api/auth/*`, `/api/me`). Refresh-token хранится в `httpOnly + Secure + SameSite=Lax` cookie `gmd_refresh`. Access-token в in-memory Zustand store. Backend URL только server-side, клиент не видит `http://localhost:3001`, CORS не нужен.

**Tech Stack:** Next.js 15 (App Router), React 19, TypeScript, Tailwind, Zustand. Никакой UI-kit — минималистичный Tailwind.

**Spec:** [docs/superpowers/specs/2026-04-18-gmd-phase1.1-web-login-design.md](../specs/2026-04-18-gmd-phase1.1-web-login-design.md)

---

## Файловая карта

```
apps/web/
├── app/
│   ├── page.tsx                             (MODIFY: добавить <Link href="/login">Войти</Link>)
│   ├── login/
│   │   └── page.tsx                         (NEW: "use client", двухстадийная форма)
│   ├── cabinet/
│   │   ├── page.tsx                         (NEW: server component + cookie check + redirect)
│   │   └── cabinet-client.tsx               (NEW: "use client", silent refresh + UI)
│   └── api/
│       ├── auth/
│       │   ├── request-otp/route.ts         (NEW: POST прокси)
│       │   ├── verify-otp/route.ts          (NEW: POST + set cookie)
│       │   ├── refresh/route.ts             (NEW: POST из cookie + обновить)
│       │   └── logout/route.ts              (NEW: POST + clear cookie)
│       └── me/route.ts                      (NEW: GET прокси)
├── lib/
│   ├── backend.ts                           (NEW: server-only fetch wrapper)
│   └── auth-store.ts                        (NEW: Zustand)
├── .env.example                             (MODIFY: BACKEND_URL)
├── .env.local                               (NEW, gitignored: BACKEND_URL)
└── package.json                             (MODIFY: + zustand)

CHANGELOG.md                                 (MODIFY: v0.4.1 запись)
```

---

## Task 1: Dependencies + env

**Files:**

- Modify: `apps/web/package.json`
- Modify: `apps/web/.env.example`
- Create: `apps/web/.env.local`

- [ ] **Step 1: Установить zustand**

```bash
cd D:/Project/GMD
pnpm --filter @gmd/web add zustand@^5.0.0
```

Expected: новая зависимость в `apps/web/package.json`, lockfile обновлён.

- [ ] **Step 2: Создать `apps/web/.env.example`**

Если файла нет — создать с содержимым:

```
# Backend URL — только server-side (Next.js API-routes)
# Dev: напрямую на NestJS. Prod: http://backend:3001 в docker-compose.
BACKEND_URL=http://127.0.0.1:3001
```

- [ ] **Step 3: Создать локальный `.env.local`**

```bash
cp apps/web/.env.example apps/web/.env.local
```

(Содержимое пойдёт такое же — URL тот же.)

Убедиться что `.env.local` игнорируется git'ом:

```bash
grep -q "\.env\*\.local" apps/web/.gitignore || echo ".env*.local" >> apps/web/.gitignore
grep ".env" apps/web/.gitignore
```

Expected: видна строка `.env*.local`.

- [ ] **Step 4: Commit**

```bash
git add apps/web/package.json pnpm-lock.yaml apps/web/.env.example apps/web/.gitignore
git commit -m "chore(web): add zustand + BACKEND_URL env"
```

---

## Task 2: Server-side backend fetch wrapper

**Files:**

- Create: `apps/web/lib/backend.ts`

- [ ] **Step 1: Написать `apps/web/lib/backend.ts`**

```typescript
import 'server-only';

const BACKEND_URL = process.env.BACKEND_URL ?? 'http://127.0.0.1:3001';

export type HttpMethod = 'GET' | 'POST' | 'PATCH' | 'DELETE';

export interface BackendResponse<T = unknown> {
  status: number;
  body: T | null;
}

export async function backend<T = unknown>(
  method: HttpMethod,
  path: string,
  body?: unknown,
  accessToken?: string,
): Promise<BackendResponse<T>> {
  const res = await fetch(`${BACKEND_URL}${path}`, {
    method,
    headers: {
      'Content-Type': 'application/json',
      ...(accessToken ? { Authorization: `Bearer ${accessToken}` } : {}),
    },
    body: body !== undefined ? JSON.stringify(body) : undefined,
    cache: 'no-store',
  });
  const text = await res.text();
  const json = text ? (JSON.parse(text) as T) : null;
  return { status: res.status, body: json };
}
```

- [ ] **Step 2: Проверить что `server-only` не ставит лишние требования**

```bash
pnpm --filter @gmd/web add server-only
```

Expected: пакет добавлен в dependencies.

- [ ] **Step 3: Typecheck**

```bash
pnpm --filter @gmd/web typecheck
```

Expected: clean.

- [ ] **Step 4: Commit**

```bash
git add apps/web/lib/backend.ts apps/web/package.json pnpm-lock.yaml
git commit -m "feat(web): server-only backend fetch wrapper"
```

---

## Task 3: Zustand auth store

**Files:**

- Create: `apps/web/lib/auth-store.ts`

- [ ] **Step 1: Написать `apps/web/lib/auth-store.ts`**

```typescript
'use client';

import { create } from 'zustand';

export interface AuthUser {
  id: string;
  email: string;
  name: string | null;
  locale: string;
}

export interface AuthFamily {
  id: string;
  name: string;
}

interface AuthState {
  accessToken: string | null;
  user: AuthUser | null;
  family: AuthFamily | null;
  setAll: (s: { accessToken: string; user: AuthUser; family: AuthFamily }) => void;
  setAccess: (t: string) => void;
  clear: () => void;
}

export const useAuthStore = create<AuthState>((set) => ({
  accessToken: null,
  user: null,
  family: null,
  setAll: ({ accessToken, user, family }) => set({ accessToken, user, family }),
  setAccess: (accessToken) => set({ accessToken }),
  clear: () => set({ accessToken: null, user: null, family: null }),
}));
```

- [ ] **Step 2: Typecheck**

```bash
pnpm --filter @gmd/web typecheck
```

Expected: clean.

- [ ] **Step 3: Commit**

```bash
git add apps/web/lib/auth-store.ts
git commit -m "feat(web): Zustand auth store (access + user + family)"
```

---

## Task 4: API-route `/api/auth/request-otp`

**Files:**

- Create: `apps/web/app/api/auth/request-otp/route.ts`

- [ ] **Step 1: Написать route**

```typescript
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';
import { backend } from '@/lib/backend';

export async function POST(req: NextRequest): Promise<NextResponse> {
  const body = (await req.json().catch(() => ({}))) as { email?: string };
  const r = await backend('POST', '/auth/request-otp', { email: body.email });
  return NextResponse.json(r.body ?? {}, { status: r.status });
}
```

- [ ] **Step 2: Проверить, что `@/` alias настроен в `tsconfig.json`**

Открыть `apps/web/tsconfig.json` — убедиться, что `compilerOptions.paths` содержит `"@/*": ["./*"]`. Если нет — добавить:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": { "@/*": ["./*"] }
  }
}
```

(В Next.js 15 стартере это обычно уже есть — проверить.)

- [ ] **Step 3: Typecheck**

```bash
pnpm --filter @gmd/web typecheck
```

Expected: clean.

- [ ] **Step 4: Smoke через curl (требует запущенный backend + web-dev)**

```bash
# backend: pnpm --filter @gmd/backend dev
# web:     pnpm --filter @gmd/web dev
curl -sS -X POST http://localhost:3003/api/auth/request-otp \
  -H 'Content-Type: application/json' \
  -d '{"email":"web-smoke@gmd.local"}' -w '\nHTTP:%{http_code}\n'
```

Expected: `{"expiresIn":600}` и `HTTP:202`.
В MailHog (`http://localhost:8025`) — новое письмо.

- [ ] **Step 5: Commit**

```bash
git add apps/web/app/api/auth/request-otp/
git commit -m "feat(web): POST /api/auth/request-otp (прокси на backend)"
```

---

## Task 5: API-route `/api/auth/verify-otp`

**Files:**

- Create: `apps/web/app/api/auth/verify-otp/route.ts`

- [ ] **Step 1: Написать route**

```typescript
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';
import { backend } from '@/lib/backend';

interface VerifyOtpBody {
  email: string;
  code: string;
}

interface BackendVerifyResponse {
  accessToken: string;
  refreshToken: string;
  user: { id: string; email: string; name: string | null; locale: string };
  family: { id: string; name: string };
}

const REFRESH_COOKIE = 'gmd_refresh';
const REFRESH_MAX_AGE = 60 * 60 * 24 * 30; // 30 дней

export async function POST(req: NextRequest): Promise<NextResponse> {
  const body = (await req.json().catch(() => ({}))) as Partial<VerifyOtpBody>;
  const r = await backend<BackendVerifyResponse>('POST', '/auth/verify-otp', {
    email: body.email,
    code: body.code,
  });
  if (r.status !== 200 || !r.body) {
    return NextResponse.json(r.body ?? {}, { status: r.status });
  }
  const { accessToken, refreshToken, user, family } = r.body;
  const res = NextResponse.json({ accessToken, user, family });
  res.cookies.set(REFRESH_COOKIE, refreshToken, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    path: '/',
    maxAge: REFRESH_MAX_AGE,
  });
  return res;
}
```

- [ ] **Step 2: Smoke — получить код из MailHog, затем verify**

```bash
# сначала request-otp (см. Task 4)
curl -sS -X POST http://localhost:3003/api/auth/request-otp -H 'Content-Type: application/json' -d '{"email":"web-verify@gmd.local"}' > /dev/null

# забрать код из MailHog (base64 декод)
CODE=$(curl -sS 'http://localhost:8025/api/v2/messages?limit=1' | python -c "
import json, sys, base64, re
msg = json.load(sys.stdin)['items'][0]
for line in msg['Content']['Body'].split('\r\n'):
    line = line.strip()
    if len(line) < 40 or '=' in line[:5] or ':' in line: continue
    try:
        d = base64.b64decode(line).decode('utf-8', errors='ignore')
        m = re.search(r'(\d{6})', d)
        if m: print(m.group(1)); break
    except: pass
")

# verify через web-прокси
curl -sS -X POST http://localhost:3003/api/auth/verify-otp \
  -H 'Content-Type: application/json' \
  -d "{\"email\":\"web-verify@gmd.local\",\"code\":\"${CODE}\"}" \
  -i -c /tmp/gmd-cookies.txt
```

Expected:

- Статус 200.
- В response body: `{accessToken, user, family}`, **refreshToken отсутствует** (он ушёл в cookie).
- В заголовках `Set-Cookie: gmd_refresh=...; HttpOnly; SameSite=lax; Path=/; Max-Age=2592000`.
- `/tmp/gmd-cookies.txt` содержит строку `gmd_refresh`.

- [ ] **Step 3: Commit**

```bash
git add apps/web/app/api/auth/verify-otp/
git commit -m "feat(web): POST /api/auth/verify-otp (set httpOnly cookie)"
```

---

## Task 6: API-route `/api/auth/refresh`

**Files:**

- Create: `apps/web/app/api/auth/refresh/route.ts`

- [ ] **Step 1: Написать route**

```typescript
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';
import { backend } from '@/lib/backend';

interface BackendRefreshResponse {
  accessToken: string;
  refreshToken: string;
}

interface BackendMeResponse {
  user: { id: string; email: string; name: string | null; locale: string };
  family: { id: string; name: string };
  memberships: Array<{ role: string; familyId: string }>;
}

const REFRESH_COOKIE = 'gmd_refresh';
const REFRESH_MAX_AGE = 60 * 60 * 24 * 30;

export async function POST(req: NextRequest): Promise<NextResponse> {
  const old = req.cookies.get(REFRESH_COOKIE)?.value;
  if (!old) {
    return NextResponse.json(
      { error: { code: 'unauthenticated', message: 'No refresh cookie' } },
      { status: 401 },
    );
  }
  const r = await backend<BackendRefreshResponse>('POST', '/auth/refresh', { refreshToken: old });
  if (r.status !== 200 || !r.body) {
    const res = NextResponse.json(r.body ?? {}, { status: r.status });
    res.cookies.delete(REFRESH_COOKIE);
    return res;
  }
  const { accessToken, refreshToken } = r.body;
  const me = await backend<BackendMeResponse>('GET', '/me', undefined, accessToken);
  const res = NextResponse.json({
    accessToken,
    ...(me.status === 200 && me.body ? { user: me.body.user, family: me.body.family } : {}),
  });
  res.cookies.set(REFRESH_COOKIE, refreshToken, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    path: '/',
    maxAge: REFRESH_MAX_AGE,
  });
  return res;
}
```

- [ ] **Step 2: Smoke**

```bash
# используем cookie из Task 5 /tmp/gmd-cookies.txt
curl -sS -X POST http://localhost:3003/api/auth/refresh \
  -b /tmp/gmd-cookies.txt \
  -c /tmp/gmd-cookies.txt \
  -i
```

Expected:

- 200 OK.
- Body: `{accessToken, user:{...}, family:{...}}`.
- Новый `Set-Cookie: gmd_refresh=...` (другое значение чем было).

- [ ] **Step 3: Smoke error path — без cookie**

```bash
curl -sS -X POST http://localhost:3003/api/auth/refresh -w '\nHTTP:%{http_code}\n'
```

Expected: `HTTP:401` и `{"error":{"code":"unauthenticated",...}}`.

- [ ] **Step 4: Commit**

```bash
git add apps/web/app/api/auth/refresh/
git commit -m "feat(web): POST /api/auth/refresh (rotate + re-read user)"
```

---

## Task 7: API-route `/api/auth/logout`

**Files:**

- Create: `apps/web/app/api/auth/logout/route.ts`

- [ ] **Step 1: Написать route**

```typescript
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';
import { backend } from '@/lib/backend';

const REFRESH_COOKIE = 'gmd_refresh';

export async function POST(req: NextRequest): Promise<NextResponse> {
  const token = req.cookies.get(REFRESH_COOKIE)?.value;
  if (token) {
    await backend('POST', '/auth/logout', { refreshToken: token });
  }
  const res = new NextResponse(null, { status: 204 });
  res.cookies.delete(REFRESH_COOKIE);
  return res;
}
```

- [ ] **Step 2: Smoke**

```bash
# logout с валидной cookie
curl -sS -X POST http://localhost:3003/api/auth/logout \
  -b /tmp/gmd-cookies.txt \
  -c /tmp/gmd-cookies.txt \
  -i
```

Expected:

- `HTTP/1.1 204 No Content`.
- `Set-Cookie: gmd_refresh=; Max-Age=0` (удалён).

Проверить что backend тоже revoke: попытка refresh после logout — 401:

```bash
curl -sS -X POST http://localhost:3003/api/auth/refresh -b /tmp/gmd-cookies.txt -w '\nHTTP:%{http_code}\n'
```

Expected: `HTTP:401`.

- [ ] **Step 3: Commit**

```bash
git add apps/web/app/api/auth/logout/
git commit -m "feat(web): POST /api/auth/logout (revoke + clear cookie)"
```

---

## Task 8: API-route `/api/me`

**Files:**

- Create: `apps/web/app/api/me/route.ts`

- [ ] **Step 1: Написать route**

```typescript
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';
import { backend } from '@/lib/backend';

export async function GET(req: NextRequest): Promise<NextResponse> {
  const auth = req.headers.get('authorization');
  if (!auth || !auth.startsWith('Bearer ')) {
    return NextResponse.json(
      { error: { code: 'unauthorized', message: 'Missing Bearer token' } },
      { status: 401 },
    );
  }
  const token = auth.slice('Bearer '.length);
  const r = await backend('GET', '/me', undefined, token);
  return NextResponse.json(r.body ?? {}, { status: r.status });
}
```

- [ ] **Step 2: Smoke (нужен свежий access)**

Этот тест делается в Task 10 (после cabinet UI). Здесь только typecheck.

```bash
pnpm --filter @gmd/web typecheck
```

Expected: clean.

- [ ] **Step 3: Commit**

```bash
git add apps/web/app/api/me/
git commit -m "feat(web): GET /api/me (прокси с Bearer)"
```

---

## Task 9: Страница `/login`

**Files:**

- Create: `apps/web/app/login/page.tsx`

- [ ] **Step 1: Написать `apps/web/app/login/page.tsx`**

```tsx
'use client';

import { useState } from 'react';
import type { FormEvent } from 'react';
import { useRouter } from 'next/navigation';
import { useAuthStore, type AuthUser, type AuthFamily } from '@/lib/auth-store';

type Stage = 'email' | 'otp';

interface VerifyResponse {
  accessToken: string;
  user: AuthUser;
  family: AuthFamily;
}

export default function LoginPage(): JSX.Element {
  const router = useRouter();
  const setAll = useAuthStore((s) => s.setAll);

  const [stage, setStage] = useState<Stage>('email');
  const [email, setEmail] = useState('');
  const [code, setCode] = useState('');
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  async function requestOtp(e: FormEvent): Promise<void> {
    e.preventDefault();
    setError(null);
    setLoading(true);
    try {
      const res = await fetch('/api/auth/request-otp', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email }),
      });
      if (res.status === 429) {
        setError('Слишком много попыток, подождите минуту');
        return;
      }
      if (res.status !== 202) {
        const json = (await res.json().catch(() => null)) as {
          error?: { message?: string };
        } | null;
        setError(json?.error?.message ?? 'Не удалось отправить код');
        return;
      }
      setStage('otp');
      setCode('');
    } catch {
      setError('Не удалось соединиться с сервером');
    } finally {
      setLoading(false);
    }
  }

  async function verifyOtp(e: FormEvent): Promise<void> {
    e.preventDefault();
    setError(null);
    setLoading(true);
    try {
      const res = await fetch('/api/auth/verify-otp', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email, code }),
      });
      const json = (await res.json().catch(() => null)) as
        | (VerifyResponse & { error?: { code?: string; message?: string } })
        | null;
      if (res.status !== 200 || !json || !('accessToken' in json) || !json.accessToken) {
        const code = json?.error?.code;
        if (code === 'invalid_code' || code === 'code_expired' || code === 'code_consumed') {
          setError('Неверный код');
        } else if (res.status === 429) {
          setError('Слишком много попыток, подождите минуту');
        } else {
          setError(json?.error?.message ?? 'Не удалось войти');
        }
        return;
      }
      setAll({ accessToken: json.accessToken, user: json.user, family: json.family });
      router.push('/cabinet');
    } catch {
      setError('Не удалось соединиться с сервером');
    } finally {
      setLoading(false);
    }
  }

  return (
    <div className="flex min-h-screen items-center justify-center bg-zinc-50 p-6">
      <div className="w-full max-w-sm rounded-lg border border-zinc-200 bg-white p-8 shadow-sm">
        <h1 className="mb-6 text-center text-2xl font-semibold text-zinc-900">Вход в GMD</h1>

        {stage === 'email' ? (
          <form onSubmit={requestOtp} className="space-y-4">
            <label className="block">
              <span className="mb-1 block text-sm font-medium text-zinc-700">Email</span>
              <input
                type="email"
                required
                autoFocus
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                className="w-full rounded-md border border-zinc-300 px-3 py-2 text-sm focus:border-zinc-900 focus:outline-none"
                placeholder="you@example.com"
              />
            </label>
            <button
              type="submit"
              disabled={loading || !email}
              className="w-full rounded-md bg-zinc-900 py-2 text-sm font-medium text-white hover:bg-zinc-800 disabled:opacity-50"
            >
              {loading ? 'Отправляем…' : 'Получить код'}
            </button>
          </form>
        ) : (
          <form onSubmit={verifyOtp} className="space-y-4">
            <p className="text-sm text-zinc-600">
              Код отправлен на <b className="text-zinc-900">{email}</b>
            </p>
            <label className="block">
              <span className="mb-1 block text-sm font-medium text-zinc-700">Код из письма</span>
              <input
                type="text"
                inputMode="numeric"
                pattern="\d{6}"
                maxLength={6}
                required
                autoFocus
                value={code}
                onChange={(e) => setCode(e.target.value.replace(/\D/g, ''))}
                className="w-full rounded-md border border-zinc-300 px-3 py-2 text-center text-lg tracking-widest focus:border-zinc-900 focus:outline-none"
                placeholder="123456"
              />
            </label>
            <button
              type="submit"
              disabled={loading || code.length !== 6}
              className="w-full rounded-md bg-zinc-900 py-2 text-sm font-medium text-white hover:bg-zinc-800 disabled:opacity-50"
            >
              {loading ? 'Входим…' : 'Войти'}
            </button>
            <button
              type="button"
              onClick={() => {
                setStage('email');
                setCode('');
                setError(null);
              }}
              className="block w-full text-center text-xs text-zinc-500 hover:text-zinc-900"
            >
              ← Изменить email / отправить код снова
            </button>
          </form>
        )}

        {error && <p className="mt-4 rounded-md bg-red-50 p-3 text-sm text-red-700">{error}</p>}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Typecheck + build**

```bash
pnpm --filter @gmd/web typecheck
pnpm --filter @gmd/web build
```

Expected: обе команды clean.

- [ ] **Step 3: Smoke через браузер**

1. `pnpm --filter @gmd/backend dev` + `pnpm --filter @gmd/web dev`.
2. Открыть `http://localhost:3003/login`.
3. Ввести `test@gmd.local` → «Получить код». Stage меняется на OTP.
4. MailHog (`http://localhost:8025`) → забрать код.
5. Ввести код → «Войти». Редирект на `/cabinet` (пока 404 — это ок, сделаем в Task 10).
6. Проверить DevTools → Application → Cookies: `gmd_refresh` с флагами `httpOnly=true`.
7. Test rate-limit: нажать «Получить код» 4 раза быстро — на 4-й показать «Слишком много попыток».
8. Test invalid code: ввести `000000` → «Неверный код».

- [ ] **Step 4: Commit**

```bash
git add apps/web/app/login/
git commit -m "feat(web): /login — двухстадийная форма email+OTP"
```

---

## Task 10: Страница `/cabinet` — server component

**Files:**

- Create: `apps/web/app/cabinet/page.tsx`
- Create: `apps/web/app/cabinet/cabinet-client.tsx`

- [ ] **Step 1: Server component `page.tsx`**

```tsx
import { cookies } from 'next/headers';
import { redirect } from 'next/navigation';
import CabinetClient from './cabinet-client';

export default async function CabinetPage(): Promise<JSX.Element> {
  const cookieStore = await cookies();
  if (!cookieStore.get('gmd_refresh')) {
    redirect('/login');
  }
  return <CabinetClient />;
}
```

- [ ] **Step 2: Client component `cabinet-client.tsx`**

```tsx
'use client';

import { useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';
import { useAuthStore, type AuthUser, type AuthFamily } from '@/lib/auth-store';

interface RefreshResponse {
  accessToken: string;
  user?: AuthUser;
  family?: AuthFamily;
}

export default function CabinetClient(): JSX.Element {
  const router = useRouter();
  const { accessToken, user, family, setAll, clear } = useAuthStore();
  const [loading, setLoading] = useState(false);
  const [bootstrapping, setBootstrapping] = useState(accessToken === null);

  useEffect(() => {
    // Silent refresh если store пуст (первый mount или F5)
    if (accessToken !== null && user !== null && family !== null) {
      setBootstrapping(false);
      return;
    }
    let cancelled = false;
    (async () => {
      try {
        const res = await fetch('/api/auth/refresh', { method: 'POST' });
        if (!res.ok) {
          router.replace('/login');
          return;
        }
        const data = (await res.json()) as RefreshResponse;
        if (cancelled) return;
        if (!data.user || !data.family) {
          router.replace('/login');
          return;
        }
        setAll({ accessToken: data.accessToken, user: data.user, family: data.family });
      } catch {
        router.replace('/login');
      } finally {
        if (!cancelled) setBootstrapping(false);
      }
    })();
    return () => {
      cancelled = true;
    };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  async function logout(): Promise<void> {
    setLoading(true);
    try {
      await fetch('/api/auth/logout', { method: 'POST' });
    } catch {
      // всё равно очищаем
    }
    clear();
    router.push('/');
  }

  if (bootstrapping) {
    return (
      <div className="flex min-h-screen items-center justify-center">
        <p className="text-sm text-zinc-500">Загружаем…</p>
      </div>
    );
  }

  if (!user || !family) {
    return (
      <div className="flex min-h-screen items-center justify-center">
        <p className="text-sm text-zinc-500">Сессия не активна</p>
      </div>
    );
  }

  return (
    <div className="flex min-h-screen items-center justify-center bg-zinc-50 p-6">
      <div className="w-full max-w-md rounded-lg border border-zinc-200 bg-white p-8 shadow-sm">
        <h1 className="mb-2 text-2xl font-semibold text-zinc-900">
          Привет, {user.name ?? user.email}!
        </h1>
        <p className="mb-6 text-sm text-zinc-600">
          Семья: <b className="text-zinc-900">{family.name}</b>
        </p>
        <div className="space-y-2 text-sm text-zinc-500">
          <p>Email: {user.email}</p>
          <p>Локаль: {user.locale}</p>
        </div>
        <button
          onClick={logout}
          disabled={loading}
          className="mt-8 w-full rounded-md bg-zinc-900 py-2 text-sm font-medium text-white hover:bg-zinc-800 disabled:opacity-50"
        >
          {loading ? 'Выходим…' : 'Выйти'}
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Typecheck + build**

```bash
pnpm --filter @gmd/web typecheck
pnpm --filter @gmd/web build
```

Expected: clean.

- [ ] **Step 4: Smoke через браузер — полный flow**

1. `http://localhost:3003/cabinet` без cookie → редирект на `/login`. ✓
2. Login полностью (email → код из MailHog → «Войти») → редирект на `/cabinet`.
3. Видно «Привет, test@gmd.local!» и «Семья: Моя семья». ✓
4. F5 на `/cabinet` → остаёмся на странице (silent refresh сработал). В Network видно POST `/api/auth/refresh` → 200. ✓
5. «Выйти» → редирект на `/`. Cookie очищен. ✓
6. Снова `/cabinet` → редирект на `/login`. ✓

- [ ] **Step 5: Commit**

```bash
git add apps/web/app/cabinet/
git commit -m "feat(web): /cabinet — silent refresh + UI + logout"
```

---

## Task 11: Лендинг — кнопка «Войти»

**Files:**

- Modify: `apps/web/app/page.tsx`

- [ ] **Step 1: Прочитать текущий `page.tsx` и добавить ссылку на `/login`**

Прочитать файл:

```bash
cat apps/web/app/page.tsx
```

Открыть в редакторе и добавить в существующую JSX-структуру (чаще всего рядом с заголовком или в header) кнопку-ссылку:

```tsx
import Link from 'next/link';

// где-то в return (...) — например в header:
<Link
  href="/login"
  className="inline-block rounded-md bg-zinc-900 px-4 py-2 text-sm font-medium text-white hover:bg-zinc-800"
>
  Войти
</Link>;
```

Принцип: НЕ переписывать весь лендинг — только добавить ссылку в видном месте.

- [ ] **Step 2: Smoke**

Открыть `http://localhost:3003/` → увидеть «Войти» → клик → попадаем на `/login`.

- [ ] **Step 3: Commit**

```bash
git add apps/web/app/page.tsx
git commit -m "feat(web): кнопка Войти на лендинге"
```

---

## Task 12: Финал — типы JSX.Element fallback + CHANGELOG v0.4.1

**Files:**

- Check: React 19 убрал `JSX.Element` из глобальной области — если typecheck падает, заменить на `React.JSX.Element` или `React.ReactElement`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Проверить typecheck полного проекта**

```bash
pnpm --filter @gmd/web typecheck
pnpm --filter @gmd/web build
pnpm --filter @gmd/web lint
```

Expected: всё clean.

Если где-то ошибка `Cannot find namespace 'JSX'` — в соответствующем файле заменить `: JSX.Element` на `: React.ReactElement` (и добавить `import type React from 'react'` вверху).

- [ ] **Step 2: Полный ручной smoke-чеклист (из spec)**

Пройти все 12 пунктов smoke-чеклиста из spec. Отметить ✓ в этом файле после прохождения.

- [ ] **Step 3: Запись в `CHANGELOG.md`**

В начало после `## [Unreleased]` добавить:

```markdown
## v0.4.1 — 2026-04-18

### Новые возможности

- **Web-кабинет: вход по email + OTP** — страницы `/login` и `/cabinet` позволяют родителю войти в систему через браузер; двухстадийная форма (email → 6-значный код → кабинет), silent refresh при F5
- **Защищённый refresh в httpOnly cookie** — refresh-token недоступен JavaScript (защита от XSS), access-token в памяти клиента (Zustand)
- **Next.js API-routes как прокси** — клиент не знает адреса backend, нет CORS; все auth-запросы идут через `/api/auth/*`

### Улучшения

- Лендинг `/` — кнопка «Войти» ведёт на `/login`

### Изменения

- chore(web): добавлена зависимость `zustand`
- chore(web): `BACKEND_URL` env-переменная для серверной стороны Next.js
```

- [ ] **Step 4: Commit + tag**

```bash
git add CHANGELOG.md
git commit -m "docs(changelog): v0.4.1 — web-кабинет login/cabinet MVP"
git tag -a v0.4.1 -m "v0.4.1 — Phase 1.1-web Login + Cabinet MVP

Next.js 15 App Router: /, /login, /cabinet.
Refresh-token в httpOnly cookie, access в Zustand.
Next.js API-routes как прокси на backend.
Минималистичный Tailwind без UI-kit.

Phase 1.1-web closed."
git tag --list v0.4.1
```

---

## Self-Review

### Spec coverage

| Spec требование                                     | Задача          |
| --------------------------------------------------- | --------------- |
| `/` с кнопкой «Войти»                               | Task 11         |
| `/login` с двумя стадиями                           | Task 9          |
| `/cabinet` server+client с silent refresh           | Task 10         |
| `/api/auth/request-otp`                             | Task 4          |
| `/api/auth/verify-otp` + set cookie                 | Task 5          |
| `/api/auth/refresh` + rotate cookie                 | Task 6          |
| `/api/auth/logout` + clear cookie                   | Task 7          |
| `/api/me` прокси                                    | Task 8          |
| Refresh в `httpOnly + Secure + SameSite=Lax` cookie | Tasks 5, 6      |
| Access в Zustand in-memory                          | Task 3 + 9 + 10 |
| `BACKEND_URL` server-side only                      | Tasks 1 + 2     |
| Минималистичный Tailwind без UI-kit                 | Tasks 9, 10, 11 |
| Manual smoke-чеклист                                | Task 12 Step 2  |
| CHANGELOG v0.4.1                                    | Task 12 Step 3  |

**Нет пропусков.**

### Placeholder scan

Нет «TBD» / «similar to» / «реализовать позже» / «добавить error handling». Все steps содержат полный код или команды.

### Type consistency

- `AuthUser` (id, email, name, locale), `AuthFamily` (id, name) — определены в Task 3, используются в Tasks 5, 6, 9, 10.
- `VerifyResponse` в Task 9 совпадает с тем что возвращает route из Task 5: `{accessToken, user, family}`.
- `RefreshResponse` в Task 10 совпадает с Task 6: `{accessToken, user?, family?}` (user/family могут отсутствовать если `/me` упал).
- Cookie name `gmd_refresh` одинаковый во всех API-routes (Tasks 5, 6, 7, 10 page).
- `maxAge: 60 * 60 * 24 * 30` одинаков в Tasks 5 и 6.

### Open concerns (не блокер)

- React 19 мог убрать `JSX.Element` — Task 12 Step 1 это ловит.
- Next.js 15 `cookies()` async — Task 10 Step 1 уже `await cookies()`.

---

## Execution

План — 12 задач. Приступаю к **Inline Execution** через executing-plans (как и Phase 1.1 backend): батчами по 3-4 задачи с чекпойнтом после каждого.
