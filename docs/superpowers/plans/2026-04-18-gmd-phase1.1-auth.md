# GMD Phase 1.1 — Auth + Family Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Реализовать passwordless email-OTP аутентификацию родителя с автосозданием Family и Membership. После выполнения плана: регистрация+вход через `/auth/request-otp` → `/auth/verify-otp`, protected endpoints `GET/PATCH /me`, `PATCH /family/:id`, JWT RS256 access + opaque refresh с rotation и replay-detection.

**Architecture:** NestJS-модули `auth`, `users`, `family`. Сервисы `OtpService` / `RefreshTokenService` / `JwtService` инкапсулируют security-логику. OTP хешируется argon2id, refresh — sha256. Email доставляется через абстракцию `OtpDeliveryProvider` (единственная impl `SmtpOtpProvider`, в dev смотрит в MailHog). Rate limiting через `@nestjs/throttler` + Redis storage. Валидация через Zod + кастомный `ZodValidationPipe`.

**Tech Stack:** NestJS 11, Prisma 5, PostgreSQL 16, Redis 7, jose (JWT), @node-rs/argon2, nodemailer, zod, testcontainers, Jest, supertest, MailHog.

**Spec:** [docs/superpowers/specs/2026-04-18-gmd-phase1.1-auth-design.md](../specs/2026-04-18-gmd-phase1.1-auth-design.md).

---

## Pre-resolved open questions

| Из spec           | Решение                                                                            |
| ----------------- | ---------------------------------------------------------------------------------- |
| argon2 library    | `@node-rs/argon2` (rust, без gyp build)                                            |
| JWT library       | `jose` (Web Crypto native, лучше типы)                                             |
| Email template    | Plain string template (одно письмо в 1.1)                                          |
| OpenAPI structure | Один файл `openapi.yaml` на Phase 1.1 (split по доменам — когда добавятся другие)  |
| Версия релиза     | **v0.4.0** (MVP не feature-complete, продолжаем 0.x; v1.0.0 — когда MVP shippable) |

## Файловая карта

```
apps/backend/
├── prisma/
│   ├── schema.prisma                        (MODIFY: User + add Family, Membership, OtpCode, RefreshToken)
│   └── migrations/20260418XXXX_auth_family/
│       └── migration.sql                    (NEW, сгенерируется prisma)
├── scripts/
│   └── auth-keygen.ts                       (NEW: RS256 keypair generator)
├── src/
│   ├── main.ts                              (MODIFY: global filter, cookie-parser)
│   ├── app.module.ts                        (MODIFY: + AuthModule, UsersModule, FamilyModule, ThrottlerModule)
│   ├── common/
│   │   ├── filters/http-exception.filter.ts (NEW)
│   │   ├── filters/http-exception.filter.spec.ts (NEW)
│   │   ├── zod/zod-validation.pipe.ts       (NEW)
│   │   ├── zod/zod-validation.pipe.spec.ts  (NEW)
│   │   └── throttler/redis-throttler.storage.ts (NEW)
│   ├── auth/
│   │   ├── auth.module.ts                   (NEW)
│   │   ├── auth.controller.ts               (NEW)
│   │   ├── auth.controller.spec.ts          (NEW — integration-ish с реальным AppModule)
│   │   ├── auth.service.ts                  (NEW)
│   │   ├── auth.service.spec.ts             (NEW)
│   │   ├── otp.service.ts                   (NEW)
│   │   ├── otp.service.spec.ts              (NEW)
│   │   ├── refresh-token.service.ts         (NEW)
│   │   ├── refresh-token.service.spec.ts    (NEW)
│   │   ├── jwt.service.ts                   (NEW)
│   │   ├── jwt.service.spec.ts              (NEW)
│   │   ├── guards/jwt-auth.guard.ts         (NEW)
│   │   ├── guards/jwt-auth.guard.spec.ts    (NEW)
│   │   ├── dto/
│   │   │   ├── request-otp.dto.ts           (NEW)
│   │   │   ├── verify-otp.dto.ts            (NEW)
│   │   │   ├── refresh.dto.ts               (NEW)
│   │   │   └── logout.dto.ts                (NEW)
│   │   └── providers/
│   │       ├── otp-delivery.provider.ts     (NEW — interface + token)
│   │       ├── smtp-otp.provider.ts         (NEW)
│   │       └── fake-otp.provider.ts         (NEW — для тестов)
│   ├── users/
│   │   ├── users.module.ts                  (NEW)
│   │   ├── users.service.ts                 (NEW)
│   │   ├── users.service.spec.ts            (NEW)
│   │   └── users.controller.ts              (NEW)
│   └── family/
│       ├── family.module.ts                 (NEW)
│       ├── family.service.ts                (NEW)
│       ├── family.service.spec.ts           (NEW)
│       └── family.controller.ts             (NEW)
├── test/
│   ├── auth.e2e-spec.ts                     (NEW)
│   ├── family.e2e-spec.ts                   (NEW)
│   ├── helpers/test-app.ts                  (NEW — bootstrap app с testcontainers)
│   └── jest-e2e.json                        (NEW или MODIFY)
├── package.json                             (MODIFY: deps + scripts)
└── .env.example                             (MODIFY: JWT/SMTP/OTP vars)

infra/docker/
├── docker-compose.dev.yml                   (MODIFY: + mailhog)
└── .env.dev.example                         (MODIFY: + MAILHOG_* ports)

packages/shared-types/
└── openapi/auth.yaml                        (NEW)

CHANGELOG.md                                 (MODIFY: v0.4.0 entry)
CLAUDE.md                                    (MODIFY: Auth — уточнить что 1.1 реализован)
```

---

## Task 1: Install dependencies + scripts

**Files:**

- Modify: `apps/backend/package.json`
- Modify: `pnpm-lock.yaml` (auto)

- [ ] **Step 1: Добавить production-deps**

```bash
cd D:/Project/GMD
pnpm --filter @gmd/backend add \
  @nestjs/throttler@^6.4.0 \
  jose@^5.9.6 \
  @node-rs/argon2@^2.0.2 \
  nodemailer@^6.9.16 \
  zod@^3.24.1 \
  cookie-parser@^1.4.7
```

- [ ] **Step 2: Добавить dev-deps**

```bash
pnpm --filter @gmd/backend add -D \
  @types/nodemailer@^6.4.17 \
  @types/cookie-parser@^1.4.8 \
  testcontainers@^10.14.0 \
  @testcontainers/postgresql@^10.14.0
```

- [ ] **Step 3: Добавить scripts в `apps/backend/package.json`**

В `"scripts"`:

```json
"auth:keygen": "ts-node scripts/auth-keygen.ts",
"test:e2e": "jest --config test/jest-e2e.json"
```

- [ ] **Step 4: Commit**

```bash
git add apps/backend/package.json pnpm-lock.yaml
git commit -m "chore(backend): add auth/jwt/otp/smtp/throttler deps"
```

---

## Task 2: Prisma schema — User/Family/Membership/OtpCode/RefreshToken

**Files:**

- Modify: `apps/backend/prisma/schema.prisma`
- Create: `apps/backend/prisma/migrations/<timestamp>_auth_family/migration.sql` (через `prisma migrate dev`)

- [ ] **Step 1: Заменить содержимое `schema.prisma`**

```prisma
generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                           String    @id @default(cuid())
  email                        String    @unique
  emailVerifiedAt              DateTime?
  name                         String?
  locale                       String    @default("ru")
  acceptedPrivacyPolicyVersion String?
  createdAt                    DateTime  @default(now())
  updatedAt                    DateTime  @updatedAt
  deletedAt                    DateTime?

  memberships   Membership[]
  refreshTokens RefreshToken[]
  otpCodes      OtpCode[]

  @@index([email])
  @@map("users")
}

model Family {
  id        String    @id @default(cuid())
  name      String    @default("Моя семья")
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  deletedAt DateTime?

  memberships Membership[]

  @@map("families")
}

enum MembershipRole {
  owner
  parent
}

model Membership {
  id        String         @id @default(cuid())
  userId    String
  familyId  String
  role      MembershipRole
  createdAt DateTime       @default(now())

  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  family Family @relation(fields: [familyId], references: [id], onDelete: Cascade)

  @@unique([userId, familyId])
  @@index([familyId])
  @@map("memberships")
}

model OtpCode {
  id         String    @id @default(cuid())
  userId     String?
  email      String
  codeHash   String
  purpose    String    @default("login")
  expiresAt  DateTime
  consumedAt DateTime?
  attempts   Int       @default(0)
  createdAt  DateTime  @default(now())

  user User? @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([email, createdAt])
  @@map("otp_codes")
}

model RefreshToken {
  id          String    @id @default(cuid())
  userId      String
  tokenHash   String    @unique
  userAgent   String?
  ipAddress   String?
  expiresAt   DateTime
  revokedAt   DateTime?
  rotatedToId String?
  createdAt   DateTime  @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, revokedAt])
  @@map("refresh_tokens")
}
```

- [ ] **Step 2: Сгенерировать и применить миграцию в dev-БД**

```bash
pnpm --filter @gmd/backend prisma migrate dev --name auth_family
```

Expected: миграция создана, применена, `prisma generate` вызван автоматически.

- [ ] **Step 3: Проверить клиент сгенерирован с новыми типами**

```bash
pnpm --filter @gmd/backend exec ts-node -e "import {PrismaClient} from '@prisma/client'; const c = new PrismaClient(); console.log(typeof c.user, typeof c.family, typeof c.membership, typeof c.otpCode, typeof c.refreshToken);"
```

Expected: `function function function function function`.

- [ ] **Step 4: Commit**

```bash
git add apps/backend/prisma/schema.prisma apps/backend/prisma/migrations/
git commit -m "feat(auth): Prisma schema for User/Family/Membership/OtpCode/RefreshToken"
```

---

## Task 3: MailHog в dev-стеке

**Files:**

- Modify: `infra/docker/docker-compose.dev.yml`
- Modify: `infra/docker/.env.dev.example`

- [ ] **Step 1: Добавить сервис в `docker-compose.dev.yml`**

В блок `services:` добавить:

```yaml
mailhog:
  image: mailhog/mailhog:v1.0.1
  container_name: gmd-mailhog-dev
  restart: unless-stopped
  ports:
    - '${MAILHOG_SMTP_PORT:-1025}:1025'
    - '${MAILHOG_UI_PORT:-8025}:8025'
```

- [ ] **Step 2: Добавить переменные в `.env.dev.example`**

```
# MailHog (dev SMTP + web UI)
MAILHOG_SMTP_PORT=1025
MAILHOG_UI_PORT=8025
```

- [ ] **Step 3: Поднять MailHog**

```bash
pnpm stack:up
```

Expected: `gmd-mailhog-dev` в `docker ps`, UI откликается.

```bash
curl -sSI http://localhost:8025/ | head -1
```

Expected: `HTTP/1.1 200 OK`.

- [ ] **Step 4: Commit**

```bash
git add infra/docker/docker-compose.dev.yml infra/docker/.env.dev.example
git commit -m "feat(infra): add MailHog to dev docker-compose"
```

---

## Task 4: JWT keypair generator

**Files:**

- Create: `apps/backend/scripts/auth-keygen.ts`
- Modify: `apps/backend/.gitignore` (создать если нет)

- [ ] **Step 1: Создать `apps/backend/.gitignore`** (если нет)

```
.secrets/
```

- [ ] **Step 2: Создать `apps/backend/scripts/auth-keygen.ts`**

```typescript
/**
 * Генерирует RS256 keypair для JWT access-токенов.
 * Private → apps/backend/.secrets/jwt-private.pem (gitignored)
 * Public  → apps/backend/.secrets/jwt-public.pem
 *
 * Prod: ключи кладутся руками в /opt/gmd/secrets/ и путь в .env.prod.
 */
import { generateKeyPairSync } from 'node:crypto';
import { mkdirSync, writeFileSync, existsSync } from 'node:fs';
import { join } from 'node:path';

const DIR = join(__dirname, '..', '.secrets');
const PRIVATE_PATH = join(DIR, 'jwt-private.pem');
const PUBLIC_PATH = join(DIR, 'jwt-public.pem');

if (existsSync(PRIVATE_PATH) && !process.argv.includes('--force')) {
  console.error(`Keys already exist at ${PRIVATE_PATH}. Re-run with --force to overwrite.`);
  process.exit(1);
}

mkdirSync(DIR, { recursive: true });

const { publicKey, privateKey } = generateKeyPairSync('rsa', {
  modulusLength: 2048,
  publicKeyEncoding: { type: 'spki', format: 'pem' },
  privateKeyEncoding: { type: 'pkcs8', format: 'pem' },
});

writeFileSync(PRIVATE_PATH, privateKey, { mode: 0o600 });
writeFileSync(PUBLIC_PATH, publicKey, { mode: 0o644 });

console.log(`Generated RS256 keypair:\n  ${PRIVATE_PATH}\n  ${PUBLIC_PATH}`);
```

- [ ] **Step 3: Сгенерировать ключи для dev**

```bash
pnpm --filter @gmd/backend auth:keygen
```

Expected: два файла в `apps/backend/.secrets/`.

- [ ] **Step 4: Проверить ключи валидные**

```bash
cd apps/backend && node -e "const {createPrivateKey,createPublicKey}=require('crypto');const {readFileSync}=require('fs');createPrivateKey(readFileSync('.secrets/jwt-private.pem'));createPublicKey(readFileSync('.secrets/jwt-public.pem'));console.log('keys OK')"
```

Expected: `keys OK`.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/scripts/auth-keygen.ts apps/backend/.gitignore
git commit -m "feat(auth): RS256 keypair generator (pnpm auth:keygen)"
```

---

## Task 5: Обновить `.env.example` backend + main.ts bootstrap

**Files:**

- Modify: `apps/backend/.env.example`
- Modify: `apps/backend/src/main.ts`

- [ ] **Step 1: Добавить auth-переменные в `.env.example`**

В конец файла:

```
# --- Auth (Phase 1.1) ---
JWT_PRIVATE_KEY_PATH=./.secrets/jwt-private.pem
JWT_PUBLIC_KEY_PATH=./.secrets/jwt-public.pem
ACCESS_TOKEN_TTL_SECONDS=900
REFRESH_TOKEN_TTL_SECONDS=2592000
OTP_TTL_SECONDS=600
OTP_MAX_ATTEMPTS=3
PRIVACY_POLICY_VERSION=1.0

# --- SMTP (Phase 1.1) ---
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=GMD <no-reply@gmd.local>
```

- [ ] **Step 2: Применить эти же переменные в локальный `apps/backend/.env`**

```bash
cp apps/backend/.env apps/backend/.env.bak 2>/dev/null || true
# Вручную добавить строки из .env.example в .env, либо:
cat apps/backend/.env.example | grep -E "^(JWT_|ACCESS_|REFRESH_|OTP_|PRIVACY_|SMTP_)" >> apps/backend/.env
```

- [ ] **Step 3: Проверить что `SMTP_PORT` совпадает с `MAILHOG_SMTP_PORT` в dev-стеке**

Прочитать `infra/docker/.env.dev` (или `.env.dev.example` если dev не копировался) и `apps/backend/.env`. Если порты разошлись — поправить `apps/backend/.env`.

- [ ] **Step 4: Commit**

```bash
git add apps/backend/.env.example
git commit -m "chore(backend): env vars for Phase 1.1 auth + smtp"
```

---

## Task 6: Unified HttpExceptionFilter

**Files:**

- Create: `apps/backend/src/common/filters/http-exception.filter.ts`
- Create: `apps/backend/src/common/filters/http-exception.filter.spec.ts`

- [ ] **Step 1: Написать failing unit-test**

`apps/backend/src/common/filters/http-exception.filter.spec.ts`:

```typescript
import { HttpException, HttpStatus } from '@nestjs/common';
import { HttpExceptionFilter } from './http-exception.filter';

function mockHost(url = '/test') {
  const res: any = { status: jest.fn().mockReturnThis(), json: jest.fn() };
  const req: any = { url };
  return {
    switchToHttp: () => ({ getResponse: () => res, getRequest: () => req }),
    res,
    req,
  };
}

describe('HttpExceptionFilter', () => {
  let filter: HttpExceptionFilter;

  beforeEach(() => {
    filter = new HttpExceptionFilter();
  });

  it('оборачивает HttpException в унифицированный формат', () => {
    const host = mockHost();
    const exc = new HttpException({ code: 'forbidden', message: 'nope' }, HttpStatus.FORBIDDEN);

    filter.catch(exc, host as any);

    expect(host.res.status).toHaveBeenCalledWith(403);
    expect(host.res.json).toHaveBeenCalledWith({
      error: { code: 'forbidden', message: 'nope' },
    });
  });

  it('для строкового HttpException использует дефолтный code', () => {
    const host = mockHost();
    const exc = new HttpException('Bad Request', HttpStatus.BAD_REQUEST);

    filter.catch(exc, host as any);

    expect(host.res.status).toHaveBeenCalledWith(400);
    expect(host.res.json).toHaveBeenCalledWith({
      error: { code: 'bad_request', message: 'Bad Request' },
    });
  });

  it('неизвестная ошибка → 500 с кодом internal_error', () => {
    const host = mockHost();
    filter.catch(new Error('boom') as any, host as any);

    expect(host.res.status).toHaveBeenCalledWith(500);
    expect(host.res.json).toHaveBeenCalledWith({
      error: { code: 'internal_error', message: 'Internal server error' },
    });
  });
});
```

- [ ] **Step 2: Запустить — должен FAIL (файла нет)**

```bash
pnpm --filter @gmd/backend test src/common/filters/http-exception.filter.spec
```

Expected: FAIL "Cannot find module './http-exception.filter'".

- [ ] **Step 3: Реализовать filter**

`apps/backend/src/common/filters/http-exception.filter.ts`:

```typescript
import {
  ArgumentsHost,
  Catch,
  ExceptionFilter,
  HttpException,
  HttpStatus,
  Logger,
} from '@nestjs/common';

type ErrorPayload = { code: string; message: string; details?: unknown };

function statusToCode(status: number): string {
  switch (status) {
    case 400:
      return 'bad_request';
    case 401:
      return 'unauthorized';
    case 403:
      return 'forbidden';
    case 404:
      return 'not_found';
    case 409:
      return 'conflict';
    case 429:
      return 'rate_limited';
    default:
      return 'internal_error';
  }
}

@Catch()
export class HttpExceptionFilter implements ExceptionFilter {
  private readonly logger = new Logger(HttpExceptionFilter.name);

  catch(exception: unknown, host: ArgumentsHost): void {
    const ctx = host.switchToHttp();
    const res = ctx.getResponse();

    if (exception instanceof HttpException) {
      const status = exception.getStatus();
      const body = exception.getResponse();
      const payload = normalize(body, status);
      res.status(status).json({ error: payload });
      return;
    }

    this.logger.error('Unhandled exception', exception as Error);
    res.status(HttpStatus.INTERNAL_SERVER_ERROR).json({
      error: { code: 'internal_error', message: 'Internal server error' },
    });
  }
}

function normalize(body: unknown, status: number): ErrorPayload {
  if (typeof body === 'string') {
    return { code: statusToCode(status), message: body };
  }
  if (body && typeof body === 'object') {
    const b = body as Record<string, unknown>;
    const code = typeof b.code === 'string' ? b.code : statusToCode(status);
    const message =
      typeof b.message === 'string'
        ? b.message
        : typeof b.error === 'string'
          ? (b.error as string)
          : 'Error';
    const details = b.details;
    return details === undefined ? { code, message } : { code, message, details };
  }
  return { code: statusToCode(status), message: 'Error' };
}
```

- [ ] **Step 4: Запустить — должен PASS**

```bash
pnpm --filter @gmd/backend test src/common/filters/http-exception.filter.spec
```

Expected: 3 passed.

- [ ] **Step 5: Подключить filter и cookie-parser глобально в `main.ts`**

Прочитать текущий `apps/backend/src/main.ts` и заменить bootstrap-функцию так, чтобы:

1. Импортировать `cookie-parser` и `HttpExceptionFilter`.
2. `app.use(cookieParser())`.
3. `app.useGlobalFilters(new HttpExceptionFilter())`.

Пример (заменить соответствующие строки — не дублировать existing `NestFactory.create`):

```typescript
import { NestFactory } from '@nestjs/core';
import cookieParser from 'cookie-parser';
import { AppModule } from './app.module';
import { HttpExceptionFilter } from './common/filters/http-exception.filter';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  app.use(cookieParser());
  app.useGlobalFilters(new HttpExceptionFilter());
  await app.listen(process.env.PORT ? Number(process.env.PORT) : 3001);
}
bootstrap();
```

- [ ] **Step 6: Запустить backend, проверить что стартует**

```bash
pnpm --filter @gmd/backend dev &
sleep 3
curl -sS http://localhost:3001/healthz
# остановить
kill %1
```

Expected: `{"status":"ok"}`.

- [ ] **Step 7: Commit**

```bash
git add apps/backend/src/common/filters/ apps/backend/src/main.ts
git commit -m "feat(common): unified HttpExceptionFilter + cookie-parser"
```

---

## Task 7: ZodValidationPipe

**Files:**

- Create: `apps/backend/src/common/zod/zod-validation.pipe.ts`
- Create: `apps/backend/src/common/zod/zod-validation.pipe.spec.ts`

- [ ] **Step 1: Failing test**

```typescript
import { BadRequestException } from '@nestjs/common';
import { z } from 'zod';
import { ZodValidationPipe } from './zod-validation.pipe';

const Schema = z.object({ email: z.string().email() });
const pipe = new ZodValidationPipe(Schema);

describe('ZodValidationPipe', () => {
  it('пропускает валидный объект', () => {
    expect(pipe.transform({ email: 'a@b.com' }, {} as any)).toEqual({ email: 'a@b.com' });
  });

  it('бросает BadRequestException с code=bad_request и details на ошибке', () => {
    try {
      pipe.transform({ email: 'not-email' }, {} as any);
      fail('should throw');
    } catch (e: any) {
      expect(e).toBeInstanceOf(BadRequestException);
      const resp = e.getResponse();
      expect(resp.code).toBe('bad_request');
      expect(Array.isArray(resp.details)).toBe(true);
      expect(resp.details[0].path).toEqual(['email']);
    }
  });
});
```

- [ ] **Step 2: Запустить — FAIL**

```bash
pnpm --filter @gmd/backend test src/common/zod/zod-validation.pipe.spec
```

- [ ] **Step 3: Implement**

```typescript
import { BadRequestException, PipeTransform } from '@nestjs/common';
import { ZodSchema, ZodError } from 'zod';

export class ZodValidationPipe<T extends ZodSchema> implements PipeTransform {
  constructor(private readonly schema: T) {}

  transform(value: unknown): unknown {
    try {
      return this.schema.parse(value);
    } catch (e) {
      if (e instanceof ZodError) {
        throw new BadRequestException({
          code: 'bad_request',
          message: 'Validation failed',
          details: e.errors.map((err) => ({ path: err.path, message: err.message })),
        });
      }
      throw e;
    }
  }
}
```

- [ ] **Step 4: PASS**

```bash
pnpm --filter @gmd/backend test src/common/zod/zod-validation.pipe.spec
```

Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/common/zod/
git commit -m "feat(common): ZodValidationPipe для DTO"
```

---

## Task 8: JwtService (RS256 sign/verify через jose)

**Files:**

- Create: `apps/backend/src/auth/jwt.service.ts`
- Create: `apps/backend/src/auth/jwt.service.spec.ts`

- [ ] **Step 1: Failing test**

```typescript
import { JwtService, JwtPayload } from './jwt.service';
import { generateKeyPairSync } from 'node:crypto';
import { writeFileSync, mkdtempSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';

function setupKeys() {
  const dir = mkdtempSync(join(tmpdir(), 'gmd-jwt-'));
  const kp = generateKeyPairSync('rsa', {
    modulusLength: 2048,
    publicKeyEncoding: { type: 'spki', format: 'pem' },
    privateKeyEncoding: { type: 'pkcs8', format: 'pem' },
  });
  const privPath = join(dir, 'p.pem');
  const pubPath = join(dir, 'pub.pem');
  writeFileSync(privPath, kp.privateKey);
  writeFileSync(pubPath, kp.publicKey);
  return { privPath, pubPath };
}

describe('JwtService', () => {
  const keys = setupKeys();
  const cfg = {
    privateKeyPath: keys.privPath,
    publicKeyPath: keys.pubPath,
    accessTtlSec: 900,
  };
  const svc = new JwtService(cfg);

  beforeAll(async () => {
    await svc.onModuleInit();
  });

  const payload: JwtPayload = {
    sub: 'user-1',
    familyId: 'fam-1',
    role: 'owner',
  };

  it('signAccessToken возвращает строку', async () => {
    const token = await svc.signAccessToken(payload);
    expect(typeof token).toBe('string');
    expect(token.split('.').length).toBe(3);
  });

  it('verify возвращает те же claims', async () => {
    const token = await svc.signAccessToken(payload);
    const decoded = await svc.verifyAccessToken(token);
    expect(decoded.sub).toBe('user-1');
    expect(decoded.familyId).toBe('fam-1');
    expect(decoded.role).toBe('owner');
  });

  it('verify бросает на tampered token', async () => {
    const token = await svc.signAccessToken(payload);
    const bad = token.slice(0, -2) + 'XX';
    await expect(svc.verifyAccessToken(bad)).rejects.toThrow();
  });

  it('verify бросает на expired token', async () => {
    const shortSvc = new JwtService({ ...cfg, accessTtlSec: -10 });
    await shortSvc.onModuleInit();
    const token = await shortSvc.signAccessToken(payload);
    await expect(shortSvc.verifyAccessToken(token)).rejects.toThrow();
  });
});
```

- [ ] **Step 2: FAIL**

```bash
pnpm --filter @gmd/backend test src/auth/jwt.service.spec
```

- [ ] **Step 3: Implement**

`apps/backend/src/auth/jwt.service.ts`:

```typescript
import { Inject, Injectable, OnModuleInit } from '@nestjs/common';
import { importPKCS8, importSPKI, SignJWT, jwtVerify, KeyLike } from 'jose';
import { readFileSync } from 'node:fs';

export interface JwtConfig {
  privateKeyPath: string;
  publicKeyPath: string;
  accessTtlSec: number;
}

export interface JwtPayload {
  sub: string;
  familyId: string;
  role: 'owner' | 'parent';
}

export const JWT_CONFIG = Symbol('JWT_CONFIG');

@Injectable()
export class JwtService implements OnModuleInit {
  private privateKey!: KeyLike;
  private publicKey!: KeyLike;

  constructor(@Inject(JWT_CONFIG) private readonly cfg: JwtConfig) {}

  async onModuleInit(): Promise<void> {
    const priv = readFileSync(this.cfg.privateKeyPath, 'utf-8');
    const pub = readFileSync(this.cfg.publicKeyPath, 'utf-8');
    this.privateKey = await importPKCS8(priv, 'RS256');
    this.publicKey = await importSPKI(pub, 'RS256');
  }

  async signAccessToken(payload: JwtPayload): Promise<string> {
    return new SignJWT({ familyId: payload.familyId, role: payload.role })
      .setProtectedHeader({ alg: 'RS256' })
      .setSubject(payload.sub)
      .setIssuedAt()
      .setExpirationTime(`${this.cfg.accessTtlSec}s`)
      .sign(this.privateKey);
  }

  async verifyAccessToken(token: string): Promise<JwtPayload> {
    const { payload } = await jwtVerify(token, this.publicKey, {
      algorithms: ['RS256'],
      clockTolerance: '30s',
    });
    return {
      sub: String(payload.sub),
      familyId: String(payload.familyId),
      role: payload.role as 'owner' | 'parent',
    };
  }
}
```

- [ ] **Step 4: PASS**

```bash
pnpm --filter @gmd/backend test src/auth/jwt.service.spec
```

Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/auth/jwt.service.ts apps/backend/src/auth/jwt.service.spec.ts
git commit -m "feat(auth): JwtService RS256 (jose)"
```

---

## Task 9: OtpService

**Files:**

- Create: `apps/backend/src/auth/otp.service.ts`
- Create: `apps/backend/src/auth/otp.service.spec.ts`

- [ ] **Step 1: Failing test**

```typescript
import { OtpService, OtpConfig } from './otp.service';
import { PrismaService } from '../prisma/prisma.service';

function makePrismaMock() {
  const otpCodes: any[] = [];
  return {
    _state: otpCodes,
    otpCode: {
      create: jest.fn(({ data }: any) => {
        const row = { id: `otp-${otpCodes.length + 1}`, attempts: 0, consumedAt: null, ...data };
        otpCodes.push(row);
        return Promise.resolve(row);
      }),
      findFirst: jest.fn(({ where, orderBy }: any) => {
        const match = otpCodes
          .filter(
            (r) => r.email === where.email && r.consumedAt === null && r.expiresAt > new Date(),
          )
          .sort((a, b) => b.createdAt.getTime() - a.createdAt.getTime());
        return Promise.resolve(match[0] ?? null);
      }),
      update: jest.fn(({ where, data }: any) => {
        const row = otpCodes.find((r) => r.id === where.id);
        Object.assign(row, data);
        return Promise.resolve(row);
      }),
      updateMany: jest.fn(({ where, data }: any) => {
        otpCodes.forEach((r) => {
          if (r.email === where.email && r.consumedAt === null) {
            Object.assign(r, data);
          }
        });
        return Promise.resolve({ count: 1 });
      }),
    },
  } as unknown as PrismaService & { _state: any[] };
}

const cfg: OtpConfig = { ttlSec: 600, maxAttempts: 3 };

describe('OtpService', () => {
  it('generate создаёт запись в БД, возвращает plain code 6 цифр', async () => {
    const prisma = makePrismaMock();
    const svc = new OtpService(prisma as any, cfg);
    const { code } = await svc.generate('a@b.com');
    expect(code).toMatch(/^\d{6}$/);
    expect((prisma as any)._state.length).toBe(1);
    expect((prisma as any)._state[0].codeHash).not.toBe(code);
  });

  it('generate инвалидирует предыдущий активный код на тот же email', async () => {
    const prisma = makePrismaMock();
    const svc = new OtpService(prisma as any, cfg);
    await svc.generate('a@b.com');
    await svc.generate('a@b.com');
    const state = (prisma as any)._state;
    expect(state[0].consumedAt).not.toBeNull();
    expect(state[1].consumedAt).toBeNull();
  });

  it('verify с правильным кодом возвращает ok=true и помечает consumedAt', async () => {
    const prisma = makePrismaMock();
    const svc = new OtpService(prisma as any, cfg);
    const { code } = await svc.generate('a@b.com');
    const r = await svc.verify('a@b.com', code);
    expect(r.ok).toBe(true);
    const row = (prisma as any)._state[0];
    expect(row.consumedAt).not.toBeNull();
  });

  it('verify с неправильным кодом инкрементит attempts', async () => {
    const prisma = makePrismaMock();
    const svc = new OtpService(prisma as any, cfg);
    await svc.generate('a@b.com');
    const r = await svc.verify('a@b.com', '000000');
    expect(r.ok).toBe(false);
    expect(r.reason).toBe('invalid_code');
    expect((prisma as any)._state[0].attempts).toBe(1);
  });

  it('после maxAttempts код invalidate, следующие попытки — invalid_code', async () => {
    const prisma = makePrismaMock();
    const svc = new OtpService(prisma as any, cfg);
    const { code } = await svc.generate('a@b.com');
    await svc.verify('a@b.com', '000000');
    await svc.verify('a@b.com', '000000');
    await svc.verify('a@b.com', '000000');
    const r = await svc.verify('a@b.com', code);
    expect(r.ok).toBe(false);
    expect(r.reason).toBe('invalid_code');
  });

  it('verify если нет активного кода — invalid_code', async () => {
    const prisma = makePrismaMock();
    const svc = new OtpService(prisma as any, cfg);
    const r = await svc.verify('a@b.com', '123456');
    expect(r.ok).toBe(false);
    expect(r.reason).toBe('invalid_code');
  });
});
```

- [ ] **Step 2: FAIL**

```bash
pnpm --filter @gmd/backend test src/auth/otp.service.spec
```

- [ ] **Step 3: Implement**

```typescript
import { Inject, Injectable } from '@nestjs/common';
import { hash, verify as argonVerify } from '@node-rs/argon2';
import { randomInt } from 'node:crypto';
import { PrismaService } from '../prisma/prisma.service';

export interface OtpConfig {
  ttlSec: number;
  maxAttempts: number;
}

export const OTP_CONFIG = Symbol('OTP_CONFIG');

export type VerifyResult =
  | { ok: true; otpId: string }
  | { ok: false; reason: 'invalid_code' | 'code_expired' | 'code_consumed' };

@Injectable()
export class OtpService {
  constructor(
    private readonly prisma: PrismaService,
    @Inject(OTP_CONFIG) private readonly cfg: OtpConfig,
  ) {}

  async generate(email: string): Promise<{ code: string; otpId: string }> {
    // Инвалидировать все активные коды на этот email
    await this.prisma.otpCode.updateMany({
      where: { email, consumedAt: null },
      data: { consumedAt: new Date() },
    });
    const code = String(randomInt(100000, 1000000));
    const codeHash = await hash(code);
    const expiresAt = new Date(Date.now() + this.cfg.ttlSec * 1000);
    const row = await this.prisma.otpCode.create({
      data: { email, codeHash, expiresAt, purpose: 'login' },
    });
    return { code, otpId: row.id };
  }

  async verify(email: string, code: string): Promise<VerifyResult> {
    const row = await this.prisma.otpCode.findFirst({
      where: { email, consumedAt: null, expiresAt: { gt: new Date() } },
      orderBy: { createdAt: 'desc' },
    });
    if (!row) return { ok: false, reason: 'invalid_code' };

    const match = await argonVerify(row.codeHash, code);
    if (!match) {
      const nextAttempts = row.attempts + 1;
      if (nextAttempts >= this.cfg.maxAttempts) {
        await this.prisma.otpCode.update({
          where: { id: row.id },
          data: { attempts: nextAttempts, consumedAt: new Date() },
        });
      } else {
        await this.prisma.otpCode.update({
          where: { id: row.id },
          data: { attempts: nextAttempts },
        });
      }
      return { ok: false, reason: 'invalid_code' };
    }

    await this.prisma.otpCode.update({
      where: { id: row.id },
      data: { consumedAt: new Date() },
    });
    return { ok: true, otpId: row.id };
  }
}
```

- [ ] **Step 4: PASS**

```bash
pnpm --filter @gmd/backend test src/auth/otp.service.spec
```

Expected: 6 passed.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/auth/otp.service.ts apps/backend/src/auth/otp.service.spec.ts
git commit -m "feat(auth): OtpService (argon2id, attempts, invalidation)"
```

---

## Task 10: RefreshTokenService

**Files:**

- Create: `apps/backend/src/auth/refresh-token.service.ts`
- Create: `apps/backend/src/auth/refresh-token.service.spec.ts`

- [ ] **Step 1: Failing test**

```typescript
import { RefreshTokenService, RefreshTokenConfig } from './refresh-token.service';

function makePrismaMock() {
  const rows: any[] = [];
  return {
    _rows: rows,
    refreshToken: {
      create: jest.fn(({ data }: any) => {
        const row = {
          id: `rt-${rows.length + 1}`,
          revokedAt: null,
          rotatedToId: null,
          createdAt: new Date(),
          ...data,
        };
        rows.push(row);
        return Promise.resolve(row);
      }),
      findUnique: jest.fn(({ where }: any) =>
        Promise.resolve(rows.find((r) => r.tokenHash === where.tokenHash) ?? null),
      ),
      update: jest.fn(({ where, data }: any) => {
        const row = rows.find((r) => r.id === where.id);
        Object.assign(row, data);
        return Promise.resolve(row);
      }),
      updateMany: jest.fn(({ where, data }: any) => {
        let count = 0;
        rows.forEach((r) => {
          if (r.userId === where.userId && r.revokedAt === null) {
            Object.assign(r, data);
            count++;
          }
        });
        return Promise.resolve({ count });
      }),
    },
  } as any;
}

const cfg: RefreshTokenConfig = { ttlSec: 86400 };

describe('RefreshTokenService', () => {
  it('create возвращает plain token и пишет tokenHash в БД', async () => {
    const p = makePrismaMock();
    const svc = new RefreshTokenService(p, cfg);
    const { token } = await svc.create('user-1', { userAgent: 'UA', ipAddress: '1.2.3.4' });
    expect(typeof token).toBe('string');
    expect(token.length).toBeGreaterThan(30);
    expect(p._rows[0].tokenHash).not.toBe(token);
    expect(p._rows[0].userId).toBe('user-1');
  });

  it('rotate: старый token → revoked + rotatedToId, возвращает новый token', async () => {
    const p = makePrismaMock();
    const svc = new RefreshTokenService(p, cfg);
    const { token: t1 } = await svc.create('user-1', {});
    const r = await svc.rotate(t1, {});
    expect(r.ok).toBe(true);
    if (!r.ok) return;
    expect(r.token).not.toBe(t1);
    expect(p._rows[0].revokedAt).not.toBeNull();
    expect(p._rows[0].rotatedToId).toBe(p._rows[1].id);
  });

  it('rotate несуществующего token → invalid', async () => {
    const p = makePrismaMock();
    const svc = new RefreshTokenService(p, cfg);
    const r = await svc.rotate('fake-token', {});
    expect(r.ok).toBe(false);
  });

  it('replay detection: повторная rotate того же token → revoke всей цепочки user', async () => {
    const p = makePrismaMock();
    const svc = new RefreshTokenService(p, cfg);
    const { token: t1 } = await svc.create('user-1', {});
    await svc.rotate(t1, {});
    const r = await svc.rotate(t1, {});
    expect(r.ok).toBe(false);
    if (r.ok) return;
    expect(r.replay).toBe(true);
    // все user-1 refresh должны быть revoked
    for (const row of p._rows.filter((x: any) => x.userId === 'user-1')) {
      expect(row.revokedAt).not.toBeNull();
    }
  });

  it('revoke сбрасывает конкретный token', async () => {
    const p = makePrismaMock();
    const svc = new RefreshTokenService(p, cfg);
    const { token } = await svc.create('user-1', {});
    await svc.revoke(token);
    expect(p._rows[0].revokedAt).not.toBeNull();
  });

  it('expired token → invalid при rotate', async () => {
    const p = makePrismaMock();
    const shortCfg: RefreshTokenConfig = { ttlSec: -10 };
    const svc = new RefreshTokenService(p, shortCfg);
    const { token } = await svc.create('user-1', {});
    const r = await svc.rotate(token, {});
    expect(r.ok).toBe(false);
  });
});
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: Implement**

```typescript
import { Inject, Injectable } from '@nestjs/common';
import { createHash, randomBytes } from 'node:crypto';
import { PrismaService } from '../prisma/prisma.service';

export interface RefreshTokenConfig {
  ttlSec: number;
}

export const REFRESH_TOKEN_CONFIG = Symbol('REFRESH_TOKEN_CONFIG');

export interface TokenMeta {
  userAgent?: string;
  ipAddress?: string;
}

export type RotateResult =
  | { ok: true; token: string; userId: string }
  | { ok: false; replay?: boolean };

function sha256(v: string): string {
  return createHash('sha256').update(v).digest('hex');
}

@Injectable()
export class RefreshTokenService {
  constructor(
    private readonly prisma: PrismaService,
    @Inject(REFRESH_TOKEN_CONFIG) private readonly cfg: RefreshTokenConfig,
  ) {}

  async create(userId: string, meta: TokenMeta): Promise<{ token: string; id: string }> {
    const token = randomBytes(32).toString('base64url');
    const tokenHash = sha256(token);
    const expiresAt = new Date(Date.now() + this.cfg.ttlSec * 1000);
    const row = await this.prisma.refreshToken.create({
      data: {
        userId,
        tokenHash,
        userAgent: meta.userAgent,
        ipAddress: meta.ipAddress,
        expiresAt,
      },
    });
    return { token, id: row.id };
  }

  async rotate(oldToken: string, meta: TokenMeta): Promise<RotateResult> {
    const tokenHash = sha256(oldToken);
    const row = await this.prisma.refreshToken.findUnique({ where: { tokenHash } });
    if (!row) return { ok: false };

    // Replay: клиент использовал уже отозванный/ротированный токен
    if (row.revokedAt !== null) {
      if (row.rotatedToId !== null) {
        // Revoke всю цепочку userId
        await this.prisma.refreshToken.updateMany({
          where: { userId: row.userId, revokedAt: null },
          data: { revokedAt: new Date() },
        });
        return { ok: false, replay: true };
      }
      return { ok: false };
    }

    if (row.expiresAt <= new Date()) return { ok: false };

    const { token: newToken, id: newId } = await this.create(row.userId, meta);
    await this.prisma.refreshToken.update({
      where: { id: row.id },
      data: { revokedAt: new Date(), rotatedToId: newId },
    });
    return { ok: true, token: newToken, userId: row.userId };
  }

  async revoke(token: string): Promise<void> {
    const tokenHash = sha256(token);
    const row = await this.prisma.refreshToken.findUnique({ where: { tokenHash } });
    if (!row || row.revokedAt !== null) return;
    await this.prisma.refreshToken.update({
      where: { id: row.id },
      data: { revokedAt: new Date() },
    });
  }

  async revokeAllForUser(userId: string): Promise<void> {
    await this.prisma.refreshToken.updateMany({
      where: { userId, revokedAt: null },
      data: { revokedAt: new Date() },
    });
  }
}
```

- [ ] **Step 4: PASS**

```bash
pnpm --filter @gmd/backend test src/auth/refresh-token.service.spec
```

Expected: 6 passed.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/auth/refresh-token.service.ts apps/backend/src/auth/refresh-token.service.spec.ts
git commit -m "feat(auth): RefreshTokenService с rotation и replay detection"
```

---

## Task 11: OtpDeliveryProvider + SmtpOtpProvider + FakeOtpProvider

**Files:**

- Create: `apps/backend/src/auth/providers/otp-delivery.provider.ts`
- Create: `apps/backend/src/auth/providers/smtp-otp.provider.ts`
- Create: `apps/backend/src/auth/providers/fake-otp.provider.ts`

- [ ] **Step 1: Interface**

`otp-delivery.provider.ts`:

```typescript
export interface OtpDeliveryProvider {
  send(to: string, code: string): Promise<void>;
}

export const OTP_DELIVERY = Symbol('OTP_DELIVERY');
```

- [ ] **Step 2: SMTP impl**

`smtp-otp.provider.ts`:

```typescript
import { Inject, Injectable, Logger } from '@nestjs/common';
import { createTransport, Transporter } from 'nodemailer';
import { OtpDeliveryProvider } from './otp-delivery.provider';

export interface SmtpConfig {
  host: string;
  port: number;
  user?: string;
  pass?: string;
  from: string;
}

export const SMTP_CONFIG = Symbol('SMTP_CONFIG');

@Injectable()
export class SmtpOtpProvider implements OtpDeliveryProvider {
  private readonly logger = new Logger(SmtpOtpProvider.name);
  private readonly transporter: Transporter;

  constructor(@Inject(SMTP_CONFIG) private readonly cfg: SmtpConfig) {
    this.transporter = createTransport({
      host: cfg.host,
      port: cfg.port,
      secure: cfg.port === 465,
      auth: cfg.user ? { user: cfg.user, pass: cfg.pass } : undefined,
    });
  }

  async send(to: string, code: string): Promise<void> {
    const subject = 'Код входа в GMD';
    const text = `Ваш код входа: ${code}\n\nКод действителен 10 минут. Если вы не пытались войти — проигнорируйте это письмо.`;
    const html = `
      <div style="font-family: -apple-system, BlinkMacSystemFont, sans-serif; max-width:480px;margin:0 auto;padding:24px">
        <h2 style="color:#111">Код входа в GMD</h2>
        <p style="font-size:32px; letter-spacing:4px; font-weight:700; color:#111; margin:24px 0">${code}</p>
        <p style="color:#555">Код действителен 10 минут. Если вы не пытались войти — проигнорируйте это письмо.</p>
      </div>
    `;
    await this.transporter.sendMail({ from: this.cfg.from, to, subject, text, html });
    this.logger.log(`OTP email sent to ${to.slice(0, 3)}***`);
  }
}
```

- [ ] **Step 3: Fake impl для тестов**

`fake-otp.provider.ts`:

```typescript
import { Injectable } from '@nestjs/common';
import { OtpDeliveryProvider } from './otp-delivery.provider';

@Injectable()
export class FakeOtpProvider implements OtpDeliveryProvider {
  public readonly sent: Array<{ to: string; code: string; at: Date }> = [];

  async send(to: string, code: string): Promise<void> {
    this.sent.push({ to, code, at: new Date() });
  }

  lastCodeFor(to: string): string | undefined {
    const entry = [...this.sent].reverse().find((e) => e.to === to);
    return entry?.code;
  }

  reset(): void {
    this.sent.length = 0;
  }
}
```

- [ ] **Step 4: Smoke sanity (без unit-теста на SMTP — ни к чему)**

```bash
pnpm --filter @gmd/backend exec tsc --noEmit
```

Expected: Clean.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/auth/providers/
git commit -m "feat(auth): OtpDeliveryProvider + SmtpOtpProvider + FakeOtpProvider"
```

---

## Task 12: JwtAuthGuard

**Files:**

- Create: `apps/backend/src/auth/guards/jwt-auth.guard.ts`
- Create: `apps/backend/src/auth/guards/jwt-auth.guard.spec.ts`

- [ ] **Step 1: Failing test**

```typescript
import { JwtAuthGuard } from './jwt-auth.guard';
import { UnauthorizedException, ExecutionContext } from '@nestjs/common';
import { JwtService } from '../jwt.service';

function ctx(auth?: string): ExecutionContext {
  const req: any = { headers: auth ? { authorization: auth } : {} };
  return {
    switchToHttp: () => ({ getRequest: () => req }),
  } as any;
}

describe('JwtAuthGuard', () => {
  const mockJwt = {
    verifyAccessToken: jest.fn(),
  } as unknown as JwtService;

  it('401 если нет Authorization', async () => {
    const guard = new JwtAuthGuard(mockJwt);
    await expect(guard.canActivate(ctx())).rejects.toThrow(UnauthorizedException);
  });

  it('401 если не Bearer', async () => {
    const guard = new JwtAuthGuard(mockJwt);
    await expect(guard.canActivate(ctx('Basic abc'))).rejects.toThrow(UnauthorizedException);
  });

  it('401 если verify бросает', async () => {
    (mockJwt.verifyAccessToken as jest.Mock).mockRejectedValueOnce(new Error('bad'));
    const guard = new JwtAuthGuard(mockJwt);
    await expect(guard.canActivate(ctx('Bearer xyz'))).rejects.toThrow(UnauthorizedException);
  });

  it('true + req.user если verify ok', async () => {
    (mockJwt.verifyAccessToken as jest.Mock).mockResolvedValueOnce({
      sub: 'u1',
      familyId: 'f1',
      role: 'owner',
    });
    const guard = new JwtAuthGuard(mockJwt);
    const c = ctx('Bearer xyz');
    expect(await guard.canActivate(c)).toBe(true);
    const req = c.switchToHttp().getRequest() as any;
    expect(req.user).toEqual({ userId: 'u1', familyId: 'f1', role: 'owner' });
  });
});
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: Implement**

```typescript
import { CanActivate, ExecutionContext, Injectable, UnauthorizedException } from '@nestjs/common';
import { JwtService } from '../jwt.service';

@Injectable()
export class JwtAuthGuard implements CanActivate {
  constructor(private readonly jwt: JwtService) {}

  async canActivate(context: ExecutionContext): Promise<boolean> {
    const req = context.switchToHttp().getRequest();
    const header: string | undefined = req.headers['authorization'];
    if (!header || !header.startsWith('Bearer ')) {
      throw new UnauthorizedException({ code: 'unauthorized', message: 'Missing Bearer token' });
    }
    const token = header.slice('Bearer '.length);
    try {
      const payload = await this.jwt.verifyAccessToken(token);
      req.user = { userId: payload.sub, familyId: payload.familyId, role: payload.role };
      return true;
    } catch {
      throw new UnauthorizedException({ code: 'unauthorized', message: 'Invalid token' });
    }
  }
}
```

- [ ] **Step 4: PASS**

```bash
pnpm --filter @gmd/backend test src/auth/guards/jwt-auth.guard.spec
```

Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/auth/guards/
git commit -m "feat(auth): JwtAuthGuard"
```

---

## Task 13: AuthService

**Files:**

- Create: `apps/backend/src/auth/auth.service.ts`
- Create: `apps/backend/src/auth/auth.service.spec.ts`

- [ ] **Step 1: Failing test**

`auth.service.spec.ts` — проверяет orchestration `verifyOtp` (auto-create User+Family+Membership при первой успешной verify, логин если user существует), `requestOtp`, `refresh` (делегирует в RefreshTokenService, снова выписывает accessToken), `logout`.

```typescript
import { AuthService } from './auth.service';
import { OtpService } from './otp.service';
import { RefreshTokenService } from './refresh-token.service';
import { JwtService } from './jwt.service';
import { FakeOtpProvider } from './providers/fake-otp.provider';

function makePrismaMock() {
  const users: any[] = [];
  const families: any[] = [];
  const memberships: any[] = [];
  const api = {
    _users: users,
    _families: families,
    _memberships: memberships,
    user: {
      findUnique: jest.fn(({ where }: any) =>
        Promise.resolve(users.find((u) => u.email === where.email && !u.deletedAt) ?? null),
      ),
    },
    $transaction: jest.fn(async (fn: any) => fn(api)),
    family: {
      create: jest.fn(({ data }: any) => {
        const row = { id: `fam-${families.length + 1}`, ...data };
        families.push(row);
        return Promise.resolve(row);
      }),
    },
    membership: {
      create: jest.fn(({ data }: any) => {
        const row = { id: `m-${memberships.length + 1}`, ...data };
        memberships.push(row);
        return Promise.resolve(row);
      }),
    },
  } as any;
  api.user.create = jest.fn(({ data }: any) => {
    const row = {
      id: `u-${users.length + 1}`,
      name: null,
      deletedAt: null,
      ...data,
    };
    users.push(row);
    return Promise.resolve(row);
  });
  return api;
}

describe('AuthService.verifyOtp', () => {
  let otpSvc: OtpService;
  let rtSvc: RefreshTokenService;
  let jwt: JwtService;
  let delivery: FakeOtpProvider;

  beforeEach(() => {
    otpSvc = { verify: jest.fn(), generate: jest.fn() } as any;
    rtSvc = { create: jest.fn().mockResolvedValue({ token: 'rt', id: 'rt-id' }) } as any;
    jwt = { signAccessToken: jest.fn().mockResolvedValue('at') } as any;
    delivery = new FakeOtpProvider();
  });

  it('первая verify: создаёт User+Family+Membership, возвращает токены', async () => {
    const prisma = makePrismaMock();
    (otpSvc.verify as jest.Mock).mockResolvedValueOnce({ ok: true, otpId: 'otp-1' });
    const svc = new AuthService(prisma, otpSvc, rtSvc, jwt, delivery, {
      privacyPolicyVersion: '1.0',
    });

    const r = await svc.verifyOtp('a@b.com', '111111', {});

    expect(r.ok).toBe(true);
    if (!r.ok) return;
    expect(r.accessToken).toBe('at');
    expect(r.refreshToken).toBe('rt');
    expect(prisma._users.length).toBe(1);
    expect(prisma._users[0].email).toBe('a@b.com');
    expect(prisma._users[0].acceptedPrivacyPolicyVersion).toBe('1.0');
    expect(prisma._families.length).toBe(1);
    expect(prisma._memberships[0].role).toBe('owner');
  });

  it('повторная verify существующего user: не создаёт новых User/Family', async () => {
    const prisma = makePrismaMock();
    // прегенерированная строка
    prisma._users.push({
      id: 'u-1',
      email: 'a@b.com',
      name: null,
      deletedAt: null,
      acceptedPrivacyPolicyVersion: '1.0',
    });
    prisma._families.push({ id: 'f-1', name: 'Моя семья' });
    prisma._memberships.push({ id: 'm-1', userId: 'u-1', familyId: 'f-1', role: 'owner' });
    (otpSvc.verify as jest.Mock).mockResolvedValueOnce({ ok: true, otpId: 'otp-1' });
    prisma.user.findUnique = jest.fn(() => Promise.resolve(prisma._users[0]));
    prisma.membership.findFirst = jest.fn(() => Promise.resolve(prisma._memberships[0]));
    prisma.family.findUnique = jest.fn(() => Promise.resolve(prisma._families[0]));
    const svc = new AuthService(prisma, otpSvc, rtSvc, jwt, delivery, {
      privacyPolicyVersion: '1.0',
    });

    const r = await svc.verifyOtp('a@b.com', '111111', {});

    expect(r.ok).toBe(true);
    expect(prisma._users.length).toBe(1);
    expect(prisma._families.length).toBe(1);
  });

  it('invalid code → ok=false', async () => {
    const prisma = makePrismaMock();
    (otpSvc.verify as jest.Mock).mockResolvedValueOnce({ ok: false, reason: 'invalid_code' });
    const svc = new AuthService(prisma, otpSvc, rtSvc, jwt, delivery, {
      privacyPolicyVersion: '1.0',
    });

    const r = await svc.verifyOtp('a@b.com', '000000', {});

    expect(r.ok).toBe(false);
    if (r.ok) return;
    expect(r.reason).toBe('invalid_code');
  });
});

describe('AuthService.requestOtp', () => {
  it('generate + delivery.send вызываются', async () => {
    const prisma = makePrismaMock();
    const otpSvc: any = { generate: jest.fn().mockResolvedValue({ code: '123456', otpId: 'o-1' }) };
    const rtSvc: any = {};
    const jwt: any = {};
    const delivery = new FakeOtpProvider();
    const svc = new AuthService(prisma, otpSvc, rtSvc, jwt, delivery, {
      privacyPolicyVersion: '1.0',
    });

    await svc.requestOtp('a@b.com');

    expect(otpSvc.generate).toHaveBeenCalledWith('a@b.com');
    expect(delivery.sent.length).toBe(1);
    expect(delivery.sent[0].code).toBe('123456');
  });
});
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: Implement**

`apps/backend/src/auth/auth.service.ts`:

```typescript
import { Inject, Injectable } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';
import { OtpService } from './otp.service';
import { RefreshTokenService, TokenMeta } from './refresh-token.service';
import { JwtService } from './jwt.service';
import { OtpDeliveryProvider, OTP_DELIVERY } from './providers/otp-delivery.provider';

export interface AuthServiceConfig {
  privacyPolicyVersion: string;
}

export const AUTH_CONFIG = Symbol('AUTH_CONFIG');

export type VerifyOtpResult =
  | {
      ok: true;
      accessToken: string;
      refreshToken: string;
      user: { id: string; email: string; name: string | null };
      family: { id: string; name: string };
    }
  | { ok: false; reason: 'invalid_code' | 'code_expired' | 'code_consumed' };

@Injectable()
export class AuthService {
  constructor(
    private readonly prisma: PrismaService,
    private readonly otp: OtpService,
    private readonly refresh: RefreshTokenService,
    private readonly jwt: JwtService,
    @Inject(OTP_DELIVERY) private readonly delivery: OtpDeliveryProvider,
    @Inject(AUTH_CONFIG) private readonly cfg: AuthServiceConfig,
  ) {}

  async requestOtp(email: string): Promise<void> {
    const { code } = await this.otp.generate(email);
    await this.delivery.send(email, code);
  }

  async verifyOtp(email: string, code: string, meta: TokenMeta): Promise<VerifyOtpResult> {
    const v = await this.otp.verify(email, code);
    if (!v.ok) return { ok: false, reason: v.reason };

    // find-or-create user + family + membership
    const { user, family } = await this.findOrCreateAccount(email);

    const accessToken = await this.jwt.signAccessToken({
      sub: user.id,
      familyId: family.id,
      role: 'owner',
    });
    const { token: refreshToken } = await this.refresh.create(user.id, meta);

    return {
      ok: true,
      accessToken,
      refreshToken,
      user: { id: user.id, email: user.email, name: user.name },
      family: { id: family.id, name: family.name },
    };
  }

  async refreshTokens(
    oldRefresh: string,
    meta: TokenMeta,
  ): Promise<
    { ok: true; accessToken: string; refreshToken: string } | { ok: false; replay?: boolean }
  > {
    const r = await this.refresh.rotate(oldRefresh, meta);
    if (!r.ok) return { ok: false, replay: r.replay };

    // для подписи access нужен familyId/role — читаем Membership
    const membership = await this.prisma.membership.findFirst({
      where: { userId: r.userId },
      orderBy: { createdAt: 'asc' },
    });
    if (!membership) return { ok: false };
    const accessToken = await this.jwt.signAccessToken({
      sub: r.userId,
      familyId: membership.familyId,
      role: membership.role as 'owner' | 'parent',
    });
    return { ok: true, accessToken, refreshToken: r.token };
  }

  async logout(refreshToken: string): Promise<void> {
    await this.refresh.revoke(refreshToken);
  }

  private async findOrCreateAccount(email: string) {
    const existing = await this.prisma.user.findUnique({ where: { email } });
    if (existing && !existing.deletedAt) {
      const m = await this.prisma.membership.findFirst({
        where: { userId: existing.id },
        orderBy: { createdAt: 'asc' },
      });
      if (!m) {
        // edge case: user без семьи. Создаём.
        return this.prisma.$transaction(async (tx: any) => {
          const family = await tx.family.create({ data: {} });
          await tx.membership.create({
            data: { userId: existing.id, familyId: family.id, role: 'owner' },
          });
          const emailVerified = !existing.emailVerifiedAt
            ? await tx.user.update({
                where: { id: existing.id },
                data: { emailVerifiedAt: new Date() },
              })
            : existing;
          return { user: emailVerified, family };
        });
      }
      const family = await this.prisma.family.findUnique({ where: { id: m.familyId } });
      const user = !existing.emailVerifiedAt
        ? await this.prisma.user.update({
            where: { id: existing.id },
            data: { emailVerifiedAt: new Date() },
          })
        : existing;
      return { user, family: family! };
    }

    // create
    return this.prisma.$transaction(async (tx: any) => {
      const user = await tx.user.create({
        data: {
          email,
          emailVerifiedAt: new Date(),
          acceptedPrivacyPolicyVersion: this.cfg.privacyPolicyVersion,
        },
      });
      const family = await tx.family.create({ data: {} });
      await tx.membership.create({
        data: { userId: user.id, familyId: family.id, role: 'owner' },
      });
      return { user, family };
    });
  }
}
```

- [ ] **Step 4: PASS**

```bash
pnpm --filter @gmd/backend test src/auth/auth.service.spec
```

Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/auth/auth.service.ts apps/backend/src/auth/auth.service.spec.ts
git commit -m "feat(auth): AuthService (requestOtp/verifyOtp/refresh/logout)"
```

---

## Task 14: DTOs (Zod)

**Files:**

- Create: `apps/backend/src/auth/dto/request-otp.dto.ts`
- Create: `apps/backend/src/auth/dto/verify-otp.dto.ts`
- Create: `apps/backend/src/auth/dto/refresh.dto.ts`
- Create: `apps/backend/src/auth/dto/logout.dto.ts`

- [ ] **Step 1: `request-otp.dto.ts`**

```typescript
import { z } from 'zod';

export const RequestOtpSchema = z.object({
  email: z.string().email().max(320).toLowerCase(),
});
export type RequestOtpDto = z.infer<typeof RequestOtpSchema>;
```

- [ ] **Step 2: `verify-otp.dto.ts`**

```typescript
import { z } from 'zod';

export const VerifyOtpSchema = z.object({
  email: z.string().email().max(320).toLowerCase(),
  code: z.string().regex(/^\d{6}$/, 'Code must be 6 digits'),
});
export type VerifyOtpDto = z.infer<typeof VerifyOtpSchema>;
```

- [ ] **Step 3: `refresh.dto.ts`**

```typescript
import { z } from 'zod';

export const RefreshSchema = z.object({
  refreshToken: z.string().min(10).max(500),
});
export type RefreshDto = z.infer<typeof RefreshSchema>;
```

- [ ] **Step 4: `logout.dto.ts`**

```typescript
import { z } from 'zod';

export const LogoutSchema = z.object({
  refreshToken: z.string().min(10).max(500),
});
export type LogoutDto = z.infer<typeof LogoutSchema>;
```

- [ ] **Step 5: Typecheck**

```bash
pnpm --filter @gmd/backend exec tsc --noEmit
```

- [ ] **Step 6: Commit**

```bash
git add apps/backend/src/auth/dto/
git commit -m "feat(auth): Zod DTOs для auth endpoints"
```

---

## Task 15: AuthController

**Files:**

- Create: `apps/backend/src/auth/auth.controller.ts`
- Create: `apps/backend/src/auth/auth.module.ts`
- Modify: `apps/backend/src/app.module.ts`

- [ ] **Step 1: `auth.controller.ts`**

```typescript
import {
  Body,
  Controller,
  HttpCode,
  HttpStatus,
  Post,
  Req,
  UnauthorizedException,
  BadRequestException,
  UsePipes,
} from '@nestjs/common';
import { Request } from 'express';
import { ZodValidationPipe } from '../common/zod/zod-validation.pipe';
import { AuthService } from './auth.service';
import { RequestOtpSchema, RequestOtpDto } from './dto/request-otp.dto';
import { VerifyOtpSchema, VerifyOtpDto } from './dto/verify-otp.dto';
import { RefreshSchema, RefreshDto } from './dto/refresh.dto';
import { LogoutSchema, LogoutDto } from './dto/logout.dto';

function extractMeta(req: Request) {
  return {
    userAgent: req.headers['user-agent'],
    ipAddress: req.ip,
  };
}

@Controller('auth')
export class AuthController {
  constructor(private readonly auth: AuthService) {}

  @Post('request-otp')
  @HttpCode(HttpStatus.ACCEPTED)
  @UsePipes(new ZodValidationPipe(RequestOtpSchema))
  async requestOtp(@Body() dto: RequestOtpDto): Promise<{ expiresIn: number }> {
    // Enumeration defense: всегда отвечаем 202 через мин. задержку.
    const started = Date.now();
    try {
      await this.auth.requestOtp(dto.email);
    } catch {
      // глушим реальную ошибку, чтобы timing был однотипным
    }
    const elapsed = Date.now() - started;
    if (elapsed < 200) await new Promise((r) => setTimeout(r, 200 - elapsed));
    return { expiresIn: 600 };
  }

  @Post('verify-otp')
  @UsePipes(new ZodValidationPipe(VerifyOtpSchema))
  async verifyOtp(@Body() dto: VerifyOtpDto, @Req() req: Request) {
    const r = await this.auth.verifyOtp(dto.email, dto.code, extractMeta(req));
    if (!r.ok)
      throw new BadRequestException({ code: r.reason, message: 'OTP verification failed' });
    return r;
  }

  @Post('refresh')
  @UsePipes(new ZodValidationPipe(RefreshSchema))
  async refresh(@Body() dto: RefreshDto, @Req() req: Request) {
    const r = await this.auth.refreshTokens(dto.refreshToken, extractMeta(req));
    if (!r.ok) {
      throw new UnauthorizedException({
        code: 'refresh_invalid',
        message: r.replay ? 'Refresh reuse detected' : 'Refresh token invalid',
      });
    }
    return r;
  }

  @Post('logout')
  @HttpCode(HttpStatus.NO_CONTENT)
  @UsePipes(new ZodValidationPipe(LogoutSchema))
  async logout(@Body() dto: LogoutDto): Promise<void> {
    await this.auth.logout(dto.refreshToken);
  }
}
```

- [ ] **Step 2: `auth.module.ts`**

```typescript
import { Module } from '@nestjs/common';
import { AuthController } from './auth.controller';
import { AuthService, AUTH_CONFIG } from './auth.service';
import { JwtService, JWT_CONFIG } from './jwt.service';
import { OtpService, OTP_CONFIG } from './otp.service';
import { RefreshTokenService, REFRESH_TOKEN_CONFIG } from './refresh-token.service';
import { SmtpOtpProvider, SMTP_CONFIG } from './providers/smtp-otp.provider';
import { OTP_DELIVERY } from './providers/otp-delivery.provider';
import { PrismaModule } from '../prisma/prisma.module';
import { JwtAuthGuard } from './guards/jwt-auth.guard';

function asNum(v: string | undefined, def: number): number {
  return v ? Number(v) : def;
}

@Module({
  imports: [PrismaModule],
  controllers: [AuthController],
  providers: [
    {
      provide: JWT_CONFIG,
      useFactory: () => ({
        privateKeyPath: process.env.JWT_PRIVATE_KEY_PATH!,
        publicKeyPath: process.env.JWT_PUBLIC_KEY_PATH!,
        accessTtlSec: asNum(process.env.ACCESS_TOKEN_TTL_SECONDS, 900),
      }),
    },
    {
      provide: OTP_CONFIG,
      useFactory: () => ({
        ttlSec: asNum(process.env.OTP_TTL_SECONDS, 600),
        maxAttempts: asNum(process.env.OTP_MAX_ATTEMPTS, 3),
      }),
    },
    {
      provide: REFRESH_TOKEN_CONFIG,
      useFactory: () => ({
        ttlSec: asNum(process.env.REFRESH_TOKEN_TTL_SECONDS, 2592000),
      }),
    },
    {
      provide: SMTP_CONFIG,
      useFactory: () => ({
        host: process.env.SMTP_HOST || 'localhost',
        port: asNum(process.env.SMTP_PORT, 1025),
        user: process.env.SMTP_USER || undefined,
        pass: process.env.SMTP_PASS || undefined,
        from: process.env.SMTP_FROM || 'GMD <no-reply@gmd.local>',
      }),
    },
    {
      provide: AUTH_CONFIG,
      useFactory: () => ({
        privacyPolicyVersion: process.env.PRIVACY_POLICY_VERSION || '1.0',
      }),
    },
    { provide: OTP_DELIVERY, useClass: SmtpOtpProvider },
    SmtpOtpProvider,
    JwtService,
    OtpService,
    RefreshTokenService,
    AuthService,
    JwtAuthGuard,
  ],
  exports: [JwtService, JwtAuthGuard, AuthService],
})
export class AuthModule {}
```

- [ ] **Step 3: Добавить `AuthModule` в `app.module.ts`**

Открыть `apps/backend/src/app.module.ts`, добавить `AuthModule` в imports.

- [ ] **Step 4: Запустить backend — должен стартовать без ошибок**

```bash
pnpm --filter @gmd/backend build
```

Expected: Clean.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/auth/auth.controller.ts apps/backend/src/auth/auth.module.ts apps/backend/src/app.module.ts
git commit -m "feat(auth): AuthController + AuthModule wired into AppModule"
```

---

## Task 16: UsersService + UsersController

**Files:**

- Create: `apps/backend/src/users/users.service.ts`
- Create: `apps/backend/src/users/users.service.spec.ts`
- Create: `apps/backend/src/users/users.controller.ts`
- Create: `apps/backend/src/users/users.module.ts`
- Modify: `apps/backend/src/app.module.ts`

- [ ] **Step 1: Failing test для UsersService**

```typescript
import { UsersService } from './users.service';

function makePrismaMock() {
  const users: any[] = [];
  return {
    _users: users,
    user: {
      findUnique: jest.fn(({ where }: any) =>
        Promise.resolve(users.find((u) => u.id === where.id) ?? null),
      ),
      update: jest.fn(({ where, data }: any) => {
        const u = users.find((x) => x.id === where.id);
        Object.assign(u, data);
        return Promise.resolve(u);
      }),
    },
    membership: {
      findMany: jest.fn(({ where }: any) => Promise.resolve([])),
    },
    refreshToken: { updateMany: jest.fn().mockResolvedValue({ count: 0 }) },
  } as any;
}

describe('UsersService', () => {
  it('getMe возвращает user + family + memberships', async () => {
    const p = makePrismaMock();
    p._users.push({ id: 'u-1', email: 'a@b.com', name: null, deletedAt: null });
    p.membership.findMany = jest.fn(() => Promise.resolve([{ role: 'owner', familyId: 'f-1' }]));
    p.family = { findUnique: jest.fn(() => Promise.resolve({ id: 'f-1', name: 'Моя семья' })) };
    const svc = new UsersService(p);

    const r = await svc.getMe('u-1', 'f-1');

    expect(r.user.email).toBe('a@b.com');
    expect(r.family.name).toBe('Моя семья');
    expect(r.memberships[0].role).toBe('owner');
  });

  it('updateMe патчит name и locale', async () => {
    const p = makePrismaMock();
    p._users.push({ id: 'u-1', email: 'a@b.com', name: null, locale: 'ru', deletedAt: null });
    const svc = new UsersService(p);

    const r = await svc.updateMe('u-1', { name: 'Ivan', locale: 'en' });

    expect(r.name).toBe('Ivan');
    expect(r.locale).toBe('en');
  });

  it('softDelete выставляет deletedAt и revoke всех refresh', async () => {
    const p = makePrismaMock();
    p._users.push({ id: 'u-1', email: 'a@b.com', deletedAt: null });
    const svc = new UsersService(p);

    await svc.softDelete('u-1');

    expect(p._users[0].deletedAt).not.toBeNull();
    expect(p.refreshToken.updateMany).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: Implement `users.service.ts`**

```typescript
import { Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';

@Injectable()
export class UsersService {
  constructor(private readonly prisma: PrismaService) {}

  async getMe(userId: string, familyId: string) {
    const [user, memberships, family] = await Promise.all([
      this.prisma.user.findUnique({ where: { id: userId } }),
      this.prisma.membership.findMany({
        where: { userId },
        select: { role: true, familyId: true },
      }),
      this.prisma.family.findUnique({ where: { id: familyId } }),
    ]);
    if (!user || user.deletedAt || !family) throw new NotFoundException({ code: 'not_found' });
    return {
      user: { id: user.id, email: user.email, name: user.name, locale: user.locale },
      family: { id: family.id, name: family.name },
      memberships,
    };
  }

  async updateMe(userId: string, patch: { name?: string; locale?: string }) {
    const user = await this.prisma.user.update({
      where: { id: userId },
      data: { name: patch.name, locale: patch.locale },
    });
    return { id: user.id, email: user.email, name: user.name, locale: user.locale };
  }

  async softDelete(userId: string): Promise<void> {
    await this.prisma.$transaction([
      this.prisma.user.update({ where: { id: userId }, data: { deletedAt: new Date() } }),
      this.prisma.refreshToken.updateMany({
        where: { userId, revokedAt: null },
        data: { revokedAt: new Date() },
      }),
    ]);
  }
}
```

- [ ] **Step 4: PASS unit-тестов**

```bash
pnpm --filter @gmd/backend test src/users/users.service.spec
```

- [ ] **Step 5: `users.controller.ts`**

```typescript
import {
  Body,
  Controller,
  Delete,
  Get,
  HttpCode,
  HttpStatus,
  Patch,
  Req,
  UseGuards,
  UsePipes,
} from '@nestjs/common';
import { z } from 'zod';
import { Request } from 'express';
import { UsersService } from './users.service';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { ZodValidationPipe } from '../common/zod/zod-validation.pipe';

const UpdateMeSchema = z
  .object({
    name: z.string().min(1).max(120).optional(),
    locale: z.string().length(2).optional(),
  })
  .strict();

interface AuthedRequest extends Request {
  user: { userId: string; familyId: string; role: 'owner' | 'parent' };
}

@Controller()
@UseGuards(JwtAuthGuard)
export class UsersController {
  constructor(private readonly users: UsersService) {}

  @Get('me')
  async me(@Req() req: AuthedRequest) {
    return this.users.getMe(req.user.userId, req.user.familyId);
  }

  @Patch('me')
  @UsePipes(new ZodValidationPipe(UpdateMeSchema))
  async update(@Req() req: AuthedRequest, @Body() dto: z.infer<typeof UpdateMeSchema>) {
    const user = await this.users.updateMe(req.user.userId, dto);
    return { user };
  }

  @Delete('me')
  @HttpCode(HttpStatus.NO_CONTENT)
  async remove(@Req() req: AuthedRequest): Promise<void> {
    await this.users.softDelete(req.user.userId);
  }
}
```

- [ ] **Step 6: `users.module.ts`**

```typescript
import { Module } from '@nestjs/common';
import { AuthModule } from '../auth/auth.module';
import { PrismaModule } from '../prisma/prisma.module';
import { UsersController } from './users.controller';
import { UsersService } from './users.service';

@Module({
  imports: [AuthModule, PrismaModule],
  controllers: [UsersController],
  providers: [UsersService],
})
export class UsersModule {}
```

- [ ] **Step 7: Регистрация в `app.module.ts`**

Добавить `UsersModule` в imports `AppModule`.

- [ ] **Step 8: Commit**

```bash
git add apps/backend/src/users/ apps/backend/src/app.module.ts
git commit -m "feat(users): GET/PATCH/DELETE /me endpoints"
```

---

## Task 17: FamilyService + FamilyController

**Files:**

- Create: `apps/backend/src/family/family.service.ts`
- Create: `apps/backend/src/family/family.service.spec.ts`
- Create: `apps/backend/src/family/family.controller.ts`
- Create: `apps/backend/src/family/family.module.ts`
- Modify: `apps/backend/src/app.module.ts`

- [ ] **Step 1: Failing test**

```typescript
import { FamilyService } from './family.service';
import { ForbiddenException, NotFoundException } from '@nestjs/common';

function makePrismaMock() {
  const families: any[] = [];
  const memberships: any[] = [];
  return {
    _families: families,
    _memberships: memberships,
    family: {
      findUnique: jest.fn(({ where }: any) =>
        Promise.resolve(families.find((f) => f.id === where.id) ?? null),
      ),
      update: jest.fn(({ where, data }: any) => {
        const f = families.find((x) => x.id === where.id);
        Object.assign(f, data);
        return Promise.resolve(f);
      }),
    },
    membership: {
      findFirst: jest.fn(({ where }: any) =>
        Promise.resolve(
          memberships.find((m) => m.userId === where.userId && m.familyId === where.familyId) ??
            null,
        ),
      ),
    },
  } as any;
}

describe('FamilyService', () => {
  it('rename owner успешно', async () => {
    const p = makePrismaMock();
    p._families.push({ id: 'f-1', name: 'Моя семья', deletedAt: null });
    p._memberships.push({ userId: 'u-1', familyId: 'f-1', role: 'owner' });
    const svc = new FamilyService(p);

    const r = await svc.rename('u-1', 'f-1', 'Кузнецовы');

    expect(r.name).toBe('Кузнецовы');
  });

  it('rename не-owner → ForbiddenException', async () => {
    const p = makePrismaMock();
    p._families.push({ id: 'f-1', name: 'Моя семья', deletedAt: null });
    p._memberships.push({ userId: 'u-1', familyId: 'f-1', role: 'parent' });
    const svc = new FamilyService(p);

    await expect(svc.rename('u-1', 'f-1', 'X')).rejects.toThrow(ForbiddenException);
  });

  it('rename несуществующей family → NotFoundException', async () => {
    const p = makePrismaMock();
    const svc = new FamilyService(p);

    await expect(svc.rename('u-1', 'f-missing', 'X')).rejects.toThrow(NotFoundException);
  });
});
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: Implement**

```typescript
import { ForbiddenException, Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';

@Injectable()
export class FamilyService {
  constructor(private readonly prisma: PrismaService) {}

  async rename(userId: string, familyId: string, name: string) {
    const family = await this.prisma.family.findUnique({ where: { id: familyId } });
    if (!family || family.deletedAt) {
      throw new NotFoundException({ code: 'not_found', message: 'Family not found' });
    }
    const m = await this.prisma.membership.findFirst({ where: { userId, familyId } });
    if (!m || m.role !== 'owner') {
      throw new ForbiddenException({ code: 'forbidden', message: 'Only owner can rename family' });
    }
    return this.prisma.family.update({ where: { id: familyId }, data: { name } });
  }
}
```

- [ ] **Step 4: PASS**

```bash
pnpm --filter @gmd/backend test src/family/family.service.spec
```

- [ ] **Step 5: Controller**

```typescript
import { Body, Controller, Param, Patch, Req, UseGuards, UsePipes } from '@nestjs/common';
import { z } from 'zod';
import { Request } from 'express';
import { FamilyService } from './family.service';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { ZodValidationPipe } from '../common/zod/zod-validation.pipe';

const PatchFamilySchema = z.object({ name: z.string().min(1).max(120) }).strict();

interface AuthedRequest extends Request {
  user: { userId: string; familyId: string; role: 'owner' | 'parent' };
}

@Controller('family')
@UseGuards(JwtAuthGuard)
export class FamilyController {
  constructor(private readonly family: FamilyService) {}

  @Patch(':id')
  @UsePipes(new ZodValidationPipe(PatchFamilySchema))
  async rename(
    @Param('id') id: string,
    @Body() dto: z.infer<typeof PatchFamilySchema>,
    @Req() req: AuthedRequest,
  ) {
    const family = await this.family.rename(req.user.userId, id, dto.name);
    return { family: { id: family.id, name: family.name } };
  }
}
```

- [ ] **Step 6: Module**

```typescript
import { Module } from '@nestjs/common';
import { AuthModule } from '../auth/auth.module';
import { PrismaModule } from '../prisma/prisma.module';
import { FamilyController } from './family.controller';
import { FamilyService } from './family.service';

@Module({
  imports: [AuthModule, PrismaModule],
  controllers: [FamilyController],
  providers: [FamilyService],
})
export class FamilyModule {}
```

- [ ] **Step 7: Регистрация в AppModule**

Добавить `FamilyModule`.

- [ ] **Step 8: Commit**

```bash
git add apps/backend/src/family/ apps/backend/src/app.module.ts
git commit -m "feat(family): PATCH /family/:id (owner-only rename)"
```

---

## Task 18: Rate limiting (Throttler + Redis storage)

**Files:**

- Create: `apps/backend/src/common/throttler/redis-throttler.storage.ts`
- Modify: `apps/backend/src/app.module.ts`
- Modify: `apps/backend/src/auth/auth.controller.ts`

- [ ] **Step 1: Написать `redis-throttler.storage.ts`**

```typescript
import { Injectable } from '@nestjs/common';
import { ThrottlerStorage } from '@nestjs/throttler';
import Redis from 'ioredis';

@Injectable()
export class RedisThrottlerStorage implements ThrottlerStorage {
  constructor(private readonly redis: Redis) {}

  async increment(
    key: string,
    ttl: number,
    limit: number,
    blockDuration: number,
    throttlerName: string,
  ): Promise<{
    totalHits: number;
    timeToExpire: number;
    isBlocked: boolean;
    timeToBlockExpire: number;
  }> {
    const k = `thr:${throttlerName}:${key}`;
    const tx = this.redis.multi();
    tx.incr(k);
    tx.pttl(k);
    const res = (await tx.exec()) as [unknown, [Error | null, number]][];
    const hits = Number((res[0] as [Error | null, number])[1]);
    let pttl = Number((res[1] as [Error | null, number])[1]);
    if (pttl < 0) {
      await this.redis.pexpire(k, ttl);
      pttl = ttl;
    }
    const isBlocked = hits > limit;
    return {
      totalHits: hits,
      timeToExpire: Math.ceil(pttl / 1000),
      isBlocked,
      timeToBlockExpire: isBlocked ? Math.ceil(pttl / 1000) : 0,
    };
  }
}
```

- [ ] **Step 2: Зарегистрировать ThrottlerModule в `AppModule`**

```typescript
import { ThrottlerGuard, ThrottlerModule } from '@nestjs/throttler';
import { APP_GUARD } from '@nestjs/core';
import Redis from 'ioredis';
import { RedisThrottlerStorage } from './common/throttler/redis-throttler.storage';

// в imports AppModule:
ThrottlerModule.forRootAsync({
  useFactory: () => ({
    throttlers: [{ name: 'global', ttl: 60_000, limit: 60 }],
    storage: new RedisThrottlerStorage(
      new Redis(process.env.REDIS_URL || 'redis://localhost:6379'),
    ),
  }),
}),

// в providers:
{ provide: APP_GUARD, useClass: ThrottlerGuard },
```

- [ ] **Step 3: Назначить per-endpoint лимиты через декораторы `@Throttle`**

В `auth.controller.ts` над методами:

```typescript
import { Throttle } from '@nestjs/throttler';

@Throttle({ default: { ttl: 600_000, limit: 3 } })  // 3 req / 10 min on same email
@Post('request-otp')
async requestOtp(...) {}

@Throttle({ default: { ttl: 600_000, limit: 10 } })  // 10 req / 10 min / IP
@Post('verify-otp')
async verifyOtp(...) {}

@Throttle({ default: { ttl: 60_000, limit: 30 } })
@Post('refresh')
async refresh(...) {}

@Throttle({ default: { ttl: 60_000, limit: 30 } })
@Post('logout')
async logout(...) {}
```

- [ ] **Step 4: Проверить что сервис стартует**

```bash
pnpm --filter @gmd/backend build
```

Expected: Clean.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/common/throttler/ apps/backend/src/app.module.ts apps/backend/src/auth/auth.controller.ts
git commit -m "feat(common): Redis-backed rate limiting via @nestjs/throttler"
```

---

## Task 19: Integration tests (e2e)

**Files:**

- Create: `apps/backend/test/helpers/test-app.ts`
- Create: `apps/backend/test/auth.e2e-spec.ts`
- Create: `apps/backend/test/family.e2e-spec.ts`
- Create: `apps/backend/test/jest-e2e.json`

- [ ] **Step 1: `jest-e2e.json`**

```json
{
  "moduleFileExtensions": ["js", "json", "ts"],
  "rootDir": ".",
  "testEnvironment": "node",
  "testRegex": ".e2e-spec\\.ts$",
  "transform": { "^.+\\.(t|j)s$": "ts-jest" },
  "testTimeout": 120000
}
```

- [ ] **Step 2: `test/helpers/test-app.ts`**

```typescript
import { INestApplication } from '@nestjs/common';
import { Test } from '@nestjs/testing';
import cookieParser from 'cookie-parser';
import { execSync } from 'node:child_process';
import { generateKeyPairSync } from 'node:crypto';
import { mkdtempSync, writeFileSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { PostgreSqlContainer, StartedPostgreSqlContainer } from '@testcontainers/postgresql';
import Redis from 'ioredis';
import { AppModule } from '../../src/app.module';
import { HttpExceptionFilter } from '../../src/common/filters/http-exception.filter';
import { FakeOtpProvider } from '../../src/auth/providers/fake-otp.provider';
import { OTP_DELIVERY } from '../../src/auth/providers/otp-delivery.provider';

export interface TestAppHandle {
  app: INestApplication;
  delivery: FakeOtpProvider;
  pg: StartedPostgreSqlContainer;
  redis: Redis;
  close: () => Promise<void>;
}

export async function bootTestApp(): Promise<TestAppHandle> {
  const pg = await new PostgreSqlContainer('postgis/postgis:16-3.4').start();
  process.env.DATABASE_URL = pg.getConnectionUri();

  // prisma migrate deploy из dev-БД — на testcontainers pg
  execSync('pnpm --filter @gmd/backend prisma migrate deploy', {
    env: { ...process.env, DATABASE_URL: pg.getConnectionUri() },
    stdio: 'inherit',
  });

  // JWT keypair temp
  const dir = mkdtempSync(join(tmpdir(), 'gmd-e2e-jwt-'));
  const kp = generateKeyPairSync('rsa', {
    modulusLength: 2048,
    publicKeyEncoding: { type: 'spki', format: 'pem' },
    privateKeyEncoding: { type: 'pkcs8', format: 'pem' },
  });
  writeFileSync(join(dir, 'p.pem'), kp.privateKey);
  writeFileSync(join(dir, 'pub.pem'), kp.publicKey);
  process.env.JWT_PRIVATE_KEY_PATH = join(dir, 'p.pem');
  process.env.JWT_PUBLIC_KEY_PATH = join(dir, 'pub.pem');
  process.env.ACCESS_TOKEN_TTL_SECONDS = '900';
  process.env.REFRESH_TOKEN_TTL_SECONDS = '60';
  process.env.OTP_TTL_SECONDS = '60';
  process.env.OTP_MAX_ATTEMPTS = '3';
  process.env.PRIVACY_POLICY_VERSION = '1.0';
  process.env.REDIS_URL = 'redis://localhost:6379';

  const delivery = new FakeOtpProvider();

  const module = await Test.createTestingModule({ imports: [AppModule] })
    .overrideProvider(OTP_DELIVERY)
    .useValue(delivery)
    .compile();

  const app = module.createNestApplication();
  app.use(cookieParser());
  app.useGlobalFilters(new HttpExceptionFilter());
  await app.init();

  const redis = new Redis(process.env.REDIS_URL);

  return {
    app,
    delivery,
    pg,
    redis,
    async close() {
      await app.close();
      await pg.stop();
      redis.disconnect();
    },
  };
}
```

- [ ] **Step 3: `auth.e2e-spec.ts`**

```typescript
import request from 'supertest';
import { bootTestApp, TestAppHandle } from './helpers/test-app';

describe('Auth (e2e)', () => {
  let h: TestAppHandle;

  beforeAll(async () => {
    h = await bootTestApp();
  }, 180_000);
  afterAll(async () => {
    await h.close();
  });

  beforeEach(async () => {
    h.delivery.reset();
    await h.redis.flushdb();
    // truncate
    const prisma = h.app.get('PrismaService') as any;
    await prisma.$executeRawUnsafe(
      'TRUNCATE TABLE refresh_tokens, otp_codes, memberships, families, users RESTART IDENTITY CASCADE;',
    );
  });

  async function getCode(email: string): Promise<string> {
    const code = h.delivery.lastCodeFor(email);
    if (!code) throw new Error(`no code for ${email}`);
    return code;
  }

  it('full flow: request-otp → verify-otp → /me → refresh → logout', async () => {
    const email = 'new@user.com';

    await request(h.app.getHttpServer()).post('/auth/request-otp').send({ email }).expect(202);
    const code = await getCode(email);
    expect(code).toMatch(/^\d{6}$/);

    const v = await request(h.app.getHttpServer())
      .post('/auth/verify-otp')
      .send({ email, code })
      .expect(200);
    expect(v.body.accessToken).toBeTruthy();
    expect(v.body.refreshToken).toBeTruthy();
    expect(v.body.user.email).toBe(email);
    expect(v.body.family.name).toBe('Моя семья');

    const me = await request(h.app.getHttpServer())
      .get('/me')
      .set('Authorization', `Bearer ${v.body.accessToken}`)
      .expect(200);
    expect(me.body.user.email).toBe(email);
    expect(me.body.memberships[0].role).toBe('owner');

    const refreshed = await request(h.app.getHttpServer())
      .post('/auth/refresh')
      .send({ refreshToken: v.body.refreshToken })
      .expect(200);
    expect(refreshed.body.refreshToken).not.toBe(v.body.refreshToken);

    await request(h.app.getHttpServer())
      .post('/auth/logout')
      .send({ refreshToken: refreshed.body.refreshToken })
      .expect(204);
  });

  it('replay detection: повторная rotate старого refresh → revoke всей цепочки', async () => {
    const email = 'replay@x.com';
    await request(h.app.getHttpServer()).post('/auth/request-otp').send({ email }).expect(202);
    const code = await getCode(email);
    const v = await request(h.app.getHttpServer())
      .post('/auth/verify-otp')
      .send({ email, code })
      .expect(200);

    const first = await request(h.app.getHttpServer())
      .post('/auth/refresh')
      .send({ refreshToken: v.body.refreshToken })
      .expect(200);

    // повторно старый refresh → 401
    await request(h.app.getHttpServer())
      .post('/auth/refresh')
      .send({ refreshToken: v.body.refreshToken })
      .expect(401);

    // новый refresh тоже должен быть revoked
    await request(h.app.getHttpServer())
      .post('/auth/refresh')
      .send({ refreshToken: first.body.refreshToken })
      .expect(401);
  });

  it('invalid code → 400 invalid_code', async () => {
    const email = 'bad@x.com';
    await request(h.app.getHttpServer()).post('/auth/request-otp').send({ email }).expect(202);
    const r = await request(h.app.getHttpServer())
      .post('/auth/verify-otp')
      .send({ email, code: '000000' })
      .expect(400);
    expect(r.body.error.code).toBe('invalid_code');
  });

  it('rate limit request-otp: 4-й запрос в окне → 429', async () => {
    const email = 'rl@x.com';
    await request(h.app.getHttpServer()).post('/auth/request-otp').send({ email }).expect(202);
    await request(h.app.getHttpServer()).post('/auth/request-otp').send({ email }).expect(202);
    await request(h.app.getHttpServer()).post('/auth/request-otp').send({ email }).expect(202);
    await request(h.app.getHttpServer()).post('/auth/request-otp').send({ email }).expect(429);
  });

  it('delete /me → access и refresh инвалидируются', async () => {
    const email = 'del@x.com';
    await request(h.app.getHttpServer()).post('/auth/request-otp').send({ email }).expect(202);
    const code = await getCode(email);
    const v = await request(h.app.getHttpServer())
      .post('/auth/verify-otp')
      .send({ email, code })
      .expect(200);

    await request(h.app.getHttpServer())
      .delete('/me')
      .set('Authorization', `Bearer ${v.body.accessToken}`)
      .expect(204);

    await request(h.app.getHttpServer())
      .post('/auth/refresh')
      .send({ refreshToken: v.body.refreshToken })
      .expect(401);
  });
});
```

- [ ] **Step 4: `family.e2e-spec.ts`**

```typescript
import request from 'supertest';
import { bootTestApp, TestAppHandle } from './helpers/test-app';

describe('Family (e2e)', () => {
  let h: TestAppHandle;

  beforeAll(async () => {
    h = await bootTestApp();
  }, 180_000);
  afterAll(async () => {
    await h.close();
  });

  beforeEach(async () => {
    h.delivery.reset();
    await h.redis.flushdb();
    const prisma = h.app.get('PrismaService') as any;
    await prisma.$executeRawUnsafe(
      'TRUNCATE TABLE refresh_tokens, otp_codes, memberships, families, users RESTART IDENTITY CASCADE;',
    );
  });

  it('owner может переименовать свою семью', async () => {
    const email = 'owner@x.com';
    await request(h.app.getHttpServer()).post('/auth/request-otp').send({ email }).expect(202);
    const v = await request(h.app.getHttpServer())
      .post('/auth/verify-otp')
      .send({ email, code: h.delivery.lastCodeFor(email)! })
      .expect(200);

    const r = await request(h.app.getHttpServer())
      .patch(`/family/${v.body.family.id}`)
      .set('Authorization', `Bearer ${v.body.accessToken}`)
      .send({ name: 'Кузнецовы' })
      .expect(200);
    expect(r.body.family.name).toBe('Кузнецовы');
  });
});
```

- [ ] **Step 5: Запустить e2e**

Требует: dev docker-стек с Redis, Docker Desktop с поднятым daemon (testcontainers).

```bash
pnpm --filter @gmd/backend test:e2e
```

Expected: все тесты пройдены.

- [ ] **Step 6: Commit**

```bash
git add apps/backend/test/
git commit -m "test(e2e): полный auth + family flow на testcontainers"
```

---

## Task 20: Smoke через MailHog + deploy + CHANGELOG v0.4.0

**Files:**

- Modify: `CHANGELOG.md`
- Modify: `CLAUDE.md`
- Modify: `apps/backend/src/app.module.ts` (если что ещё не зарегистрировано)

- [ ] **Step 1: Dev smoke через MailHog**

```bash
# стек поднят через pnpm stack:up
pnpm --filter @gmd/backend dev &
sleep 5

# 1) request-otp
curl -sS -X POST http://localhost:3001/auth/request-otp \
  -H 'Content-Type: application/json' \
  -d '{"email":"test@gmd.local"}' | jq

# 2) достать код из MailHog (Web UI http://localhost:8025)
#    либо через API:
CODE=$(curl -sS http://localhost:8025/api/v2/messages?limit=1 | jq -r '.items[0].Content.Body' | grep -oE '[0-9]{6}' | head -1)
echo "Code: $CODE"

# 3) verify-otp
curl -sS -X POST http://localhost:3001/auth/verify-otp \
  -H 'Content-Type: application/json' \
  -d "{\"email\":\"test@gmd.local\",\"code\":\"${CODE}\"}" | jq

kill %1
```

Expected: шаг 3 возвращает `accessToken`, `refreshToken`, `user`, `family`.

- [ ] **Step 2: Prod deploy и миграция**

```bash
bash infra/deploy/deploy.sh
```

Expected: миграция `auth_family` применилась, сервисы healthy.

- [ ] **Step 3: `CHANGELOG.md` — запись v0.4.0**

Добавить в начало (после `[Unreleased]`):

```markdown
## v0.4.0 — 2026-04-18

### Новые возможности

- **Passwordless email-OTP аутентификация** — вход по email с 6-значным кодом, без паролей; поддомен `/auth/request-otp` и `/auth/verify-otp`
- **JWT access + opaque refresh** — RS256 access-token 15m, 32-byte refresh-token 30d с rotation on use и replay detection
- **Контекст семьи по умолчанию** — при первой регистрации автоматически создаётся `Family` и `Membership(role=owner)`; `GET /me` возвращает user + family + memberships
- **Переименование семьи** — `PATCH /family/:id` доступен только owner'у
- **Soft-delete аккаунта (152-ФЗ)** — `DELETE /me` проставляет `deletedAt` + revoke всех активных refresh-токенов; hard-delete через 30 дней — отдельный job (Phase 1.3)
- **MailHog в dev-стеке** — локальный SMTP + Web UI на `http://localhost:8025`

### Улучшения

- **Rate limiting через Redis** — лимиты на `/auth/request-otp` (3/10 min), `/auth/verify-otp` (10/10 min), refresh/logout (30/min)
- **Единый формат ошибок** — `{ error: { code, message, details? } }` через глобальный `HttpExceptionFilter`
- **Zod DTO** — все входные endpoints валидируются `ZodValidationPipe`
- **Protection against enumeration** — `request-otp` всегда `202` + минимальная задержка 200ms против timing attacks

### Изменения

- feat(auth): Prisma schema `users`, `families`, `memberships`, `otp_codes`, `refresh_tokens`
- chore(deps): `@nestjs/throttler`, `jose`, `@node-rs/argon2`, `nodemailer`, `zod`, `testcontainers`
- test: unit-тесты для всех auth-сервисов + e2e на testcontainers
```

- [ ] **Step 4: `CLAUDE.md` — проставить "выполнено" для вопроса регистрации**

Найти секцию «Открытые вопросы» и заменить `4. Регистрация: email vs phone + OTP` на:

```markdown
4. ~~Регистрация: email vs phone + OTP~~ → **email + OTP** (реализовано в Phase 1.1)
```

- [ ] **Step 5: Git tag**

```bash
git add CHANGELOG.md CLAUDE.md
git commit -m "docs(changelog): v0.4.0 — email-OTP auth, JWT, Family"
git tag -a v0.4.0 -m "v0.4.0 — Phase 1.1 Auth + Family"
```

---

## Self-Review

Проверка плана против spec:

| Spec-раздел                                                         | Покрытие          |
| ------------------------------------------------------------------- | ----------------- |
| Data model (User/Family/Membership/OtpCode/RefreshToken)            | Task 2            |
| OTP генерация + argon2id + attempts + invalidation                  | Task 9            |
| Refresh rotation + replay detection                                 | Task 10           |
| JWT RS256 sign/verify + clock skew                                  | Task 8            |
| Email delivery (SMTP + Fake)                                        | Task 11           |
| MailHog в dev                                                       | Task 3            |
| JwtAuthGuard                                                        | Task 12           |
| AuthService orchestration + auto-create Family                      | Task 13           |
| Endpoints request-otp / verify-otp / refresh / logout               | Tasks 14+15       |
| GET/PATCH/DELETE /me                                                | Task 16           |
| PATCH /family/:id                                                   | Task 17           |
| Unified error filter + Zod DTO                                      | Tasks 6+7         |
| Rate limiting + Redis                                               | Task 18           |
| Integration tests (full flow + replay + rate-limit + delete)        | Task 19           |
| 152-ФЗ: acceptedPrivacyPolicyVersion + soft-delete + refresh revoke | Tasks 2 + 13 + 16 |
| Enumeration defense (202 + 200ms delay)                             | Task 15           |
| Deploy + CHANGELOG v0.4.0                                           | Task 20           |

**Type consistency check:** `JwtPayload` (Task 8) → `{sub, familyId, role}` используется в `JwtAuthGuard` (Task 12) и `AuthService` (Task 13) с одинаковыми именами. `MembershipRole` enum — `'owner' | 'parent'` везде. `VerifyResult.reason` в `OtpService` — `'invalid_code' | 'code_expired' | 'code_consumed'` совпадает с `VerifyOtpResult.reason` в `AuthService` и error code в controller'е.

**Placeholder scan:** Нет TBD / TODO / «реализовать позже» в ходе задач.

**Scope check:** атомарно — одна фаза, полностью testable, реальная prod-деплой после Task 20.

**Известные отложенные (out of scope):**

- Hard-delete job 30d (Phase 1.3)
- OpenAPI-spec файл и codegen (отложен — в 1.1 не критичен; контракт покрыт e2e). **Добавить в 1.2 когда появится Child API.**
- Контракт-тесты `@apidevtools/swagger-parser` (следует за OpenAPI).
- Экран «Активные сессии» в web (UI — Phase 1.4 Web Cabinet).
- GlitchTip / Sentry SDK wiring — Phase 0.4.
