name: gmd_shared
description: Shared Dart models (generated from OpenAPI in Phase 1)
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
